# Independent coach evaluation — AG4

`dev-cases.v1.json` contains 37 synthetic development specifications, not executed model results. Inputs and overrides describe test-harness setup; never send fixtures, identities or transport controls in a public request body. The API adapter is pending the implementation contract in AG3.

Required gates: actual repository read spies for tenant boundaries/revocation, zero productive tool actions, strict schema, authorized references plus deterministic numeric grounding, honest partial/failure states, cancellation and budget accounting. Output-only checks cannot prove denied reads. Health examples require separate human review and do not establish clinical certification. Style/usefulness is separate from correctness.

Holdouts are private outside the repository, separately sealed before candidate evaluation. They must not enter author prompts or development runners; cases used for corrections transition to development before reuse. No paid runner here; AG3 is sole shared runner and effective current cap is US$0.

Fixture arithmetic: two meals total1000 kcal and75g protein; performed volume720 kg·reps; planned30 versus performed18 repetitions. These values come from fixture arithmetic, never a judge LLM. D37 explicitly challenges wrong numbers with authorized citations.
