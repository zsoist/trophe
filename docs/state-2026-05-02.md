# Trophē State Audit — 2026-05-02

> **Snapshot date**: 2026-05-02. State changes daily. Re-run audit
> before any production-related decision. This document is not
> self-updating.

Read-only diagnostic. No code changes, no deploys, no fixes.
Every claim cites its evidence (test output, DB query, HTTP response, git log).

---

## TL;DR

- **Production deploy**: `dpl_BExuBagSC3FUhNGsrd4H9gumqd2g`, Apr 22 (10 days ago), from `main`
- **v0.3-overhaul**: 47 commits ahead of main, 241 files changed (+65,958 / -8,304 lines)
- **CI status**: 3 consecutive green runs on v0.3-overhaul (run 25251519709)
- **30-day AI spend**: $0.00 tracked locally (agent_runs table empty — cost tracking tables exist but no production writes reach local DB)
- **Verified working features**: 9
- **Code exists, behavior unverified**: 5
- **Known broken or absent**: 2 (Langfuse trace ingestion, production auth gate)
- **Active test suite**: 106 passed, 17 skipped, 1 failed (rls.test.ts — superuser requirement, known)

---

## Production vs Branch

### Current production state

| Field | Value | Evidence |
|-------|-------|----------|
| Deploy ID | `dpl_BExuBagSC3FUhNGsrd4H9gumqd2g` | `vercel inspect` output |
| Deploy date | 2026-04-22 | `vercel ls --environment production` |
| Branch | `main` (commit `6a8ab3a` or earlier) | `git log --oneline origin/main -1` |
| CSP | Still includes `unsafe-eval` | `curl -sI https://trophe-mu.vercel.app \| grep content-security-policy` |
| `/dashboard` auth gate | None (returns 200 without login) | `curl -sI https://trophe-mu.vercel.app/dashboard` returns HTTP 200 |

### Branch divergence

| Metric | Value |
|--------|-------|
| Commits ahead | 47 |
| Files changed | 241 |
| Lines added | +65,958 |
| Lines removed | -8,304 |
| Net new | +57,654 lines |

### Breaking changes for production cutover

- **Schema**: 6 Drizzle migration files (`0000`–`0005`), 24 schema definition files. Local DB has 33 tables. Production Supabase has the old hand-curated schema. Migrations must run against production before deploy.
- **Auth**: `proxy.ts` replaces `middleware.ts` for server-side session checks. `@supabase/ssr` replaces client-side-only `@supabase/supabase-js`. Auth tokens move from localStorage to HTTP-only cookies.
- **API routes**: 7 new routes (`magic-link`, `callback`, `signout`, `trpc`, 3x Spike integration). 5 modified routes (`meal-suggest`, `photo-analyze`, `food/parse`, `recipe-analyze`, `signup`).
- **New env vars required**: `DATABASE_URL` (mandatory for all DB access), `PGPASSWORD` (for ingest scripts), `GEMINI_API_KEY` (for meal_suggest + food_parse), `VOYAGE_API_KEY` (for food embeddings), `LANGFUSE_*` (for observability). See `.env.local.example`.
- **Node.js**: Requires Node 20 (unchanged from main).

---

## Feature Inventory

| Feature | Status | Evidence |
|---------|--------|----------|
| **Auth: signup/login/logout** | ✅ Verified | `tests/auth/role-gate.test.ts` — 13 test cases, 247 lines. Routes exist: signup, callback, magic-link, signout |
| **Auth: role enforcement** | ✅ Verified | `lib/auth/require-role.ts` (67 lines), `proxy.ts` (79 lines). Role hierarchy: super_admin > admin > coach > client. E2E test confirms `/dashboard` redirects to `/login` for anonymous users |
| **Food parse (v4 deterministic)** | ✅ Verified | 27/27 accuracy golden tests pass against Mac Mini DB (`npx vitest run tests/agents/food-parse.accuracy.test.ts`). LLM identifies food, DB supplies macros via `agents/food-parse/lookup.ts` (377 lines) |
| **Food data (USDA + HHF)** | ✅ Verified | `SELECT count(*) FROM foods` = 7,918 rows. 89 food aliases, 38 unit conversions. HNSW + GIN indexes confirmed |
| **LLM router** | ✅ Verified | `tests/agents/router.test.ts` — 14/14 pass. Policies: food_parse → Gemini 2.5 Flash, recipe → Haiku 4.5, coach → Sonnet 4.6, meal_suggest → Gemini 2.0 Flash |
| **Recipe analyze** | ✅ Verified | Eval suite: 3/3 pass (100%), avg 4030ms. Uses Anthropic Haiku with prompt caching |
| **Coach insight** | ✅ Verified | Eval suite: 3/3 pass (100%), avg 3322ms. Structural + content checks |
| **Enterprise invariants** | ✅ Verified | `tests/enterprise/invariants.test.ts` — 3/3 pass. Guards: no `.single()`, no hardcoded models outside router, no `dangerouslySetInnerHTML` outside layout |
| **E2E smoke tests** | ✅ Verified | 8 Playwright tests pass in CI (14.4s). Mobile + desktop. Landing, login, dashboard redirect, overflow |
| **Trilingual UI (en/es/el)** | 🟡 Code exists | `lib/i18n.tsx` — EN: 456 keys, ES: 455 keys, EL: 459 keys. Near-parity. Language toggle exists. Not verified in browser this session |
| **Coach dashboard** | 🟡 Code exists | 9 pages, ~5,186 lines total. Client view, memory, plan editor, inbox, habits, templates, foods, protocols. No automated tests for coach UI |
| **Meal suggest** | 🟡 Code exists | 184-line Gemini REST handler with fallback suggestions. Routed via `pick('meal_suggest')`. No eval suite yet (deadline: 2026-05-09) |
| **Photo analyze** | 🟡 Code exists | Route exists, uses `pick('photo_analyze')`. No dedicated eval or test |
| **Memory system** | 🟡 Code exists | `agents/memory/{read,write}.ts` (624 lines), `tests/agents/memory.test.ts` (316 lines). Schema exists. 0 rows in `memory_chunks` table — never exercised against real data |
| **Workouts / form check** | 🟡 Code exists | Pages exist. MediaPipe references in `app/demo/page.tsx`. 113 exercises seeded. No automated tests |
| **Wearables (Spike)** | 🟡 Code exists | 4 API routes (471 lines), webhook HMAC verification (5 references), `tests/spike/webhook.test.ts` (187 lines). Schema tables exist but 0 rows — no real device connected |
| **tRPC** | 🟡 Code exists | `lib/trpc/` (237 lines), route handler exists. Not clear how many coach pages actually use tRPC vs direct fetch |
| **Client dashboard** | 🟡 Code exists | Dashboard pages for mood, weight, food log, water, supplements, habits exist. Not verified in browser this session |
| **Langfuse traces** | ❌ Not reaching local DB | `agent_runs` table: 0 rows. Eval runs print OTel-style JSON to stdout but don't INSERT into the table. Langfuse server is healthy (`{"status":"OK","version":"2.95.11"}`) but no traces from app code appear to be landing |
| **Production auth gate** | ⚠️ Shell exposed, data safe | `/dashboard` returns 200 with skeleton UI (no user data in SSR). Auth is client-side only (`localStorage` JWT + Supabase RLS). Data routes return 404 or require valid JWT. One AI route (`meal-suggest`) accepts anonymous POSTs (cost-abuse risk). v0.3-overhaul fixes with server-side `proxy.ts` |

---

## Integration Health

| Service | Endpoint | Auth | Result | Notes |
|---------|----------|------|--------|-------|
| **Local Postgres** (Mac Mini) | `127.0.0.1:5433/trophe_dev` | `trophe_user` password | ✅ Working | 33 tables, 7918 foods, all grants verified |
| **Supabase production** | `iwbpzwmidzvpiofnqexd.supabase.co/rest/v1/` | Anon key | ✅ Working | HTTP 200 when both `apikey` and `Authorization: Bearer` headers sent (PostgREST requires both). Initial 401 was a testing error — missing the `Authorization` header. Anon key is valid. |
| **Anthropic API** | `api.anthropic.com/v1/models` | API key from `.env.local` | ✅ Working | 9 models returned |
| **Google Gemini** | `generativelanguage.googleapis.com/v1beta/models` | API key from `.env.local` | ✅ Working | 50 models returned |
| **Voyage AI** | `api.voyageai.com/v1/embeddings` | Key from `~/.local/secrets/voyage.env` | ❌ Invalid key | "Provided API key is invalid." Key may have been rotated |
| **Langfuse** | `127.0.0.1:3002/api/public/health` | N/A | ✅ Working | v2.95.11, status OK |
| **Spike API** | `api.spike.sh/v3/users` | Bearer test | ❓ Untested | Returns `{"page_found":false}`. No real API key configured yet |
| **Vercel** | `vercel whoami` | CLI auth | ✅ Working | Account `2p6y54z6w9-4465` |
| **GitHub** | `gh auth status` | Keyring token | ✅ Working | Account `zsoist`, SSH protocol |
| **OpenBrain** | `127.0.0.1:5433/open_brain` | `brain_user` | ✅ Working | 2016 raw_captures, verified untouched |

---

## Cost Reality

### Local DB (Mac Mini `trophe_dev`)

| Table | Rows |
|-------|------|
| `agent_runs` | 0 |
| `api_usage_log` | 0 |

**No cost data exists in the local DB.** The cost tracking schema is in place (`agent_runs`, `api_usage_log` tables exist with correct columns), but:

1. The dev server hasn't been run in a way that exercises the logging path
2. Production (Supabase) has a separate database — cost data from testers' production usage lives there, not here
3. The eval runner prints OTel-style cost traces to stdout but doesn't INSERT into these tables

### Eval session costs (just measured)

| Suite | Calls | Model | Approx cost |
|-------|-------|-------|-------------|
| recipe_analyze | 3 | claude-haiku-4-5 | ~$0.003 |
| coach_insight | 3 | claude-haiku-4-5 / claude-sonnet-4-5 | ~$0.01 |
| **Total** | **6** | | **~$0.013** |

### Budget estimate (from `policies.ts` header comment)

At 50 active users (50 meals/day, 1 coach call/day):
- food_parse: ~$0.019/day (Gemini 2.5 Flash)
- recipe: ~$0.031/day (Haiku 4.5)
- coach_insight: ~$0.12/day (Sonnet 4.6)
- **Total: ~$0.17/day (~$5/month)**

Current scale (5 testers): roughly $0.02-0.03/day expected. **Cannot verify** — production cost data is on Supabase, not queryable from this session without production DB credentials.

---

## Accuracy Reality

### Automated test results (2026-05-02)

| Test | Pass | Fail | Skip | Rate | Notes |
|------|------|------|------|------|-------|
| Food-parse accuracy goldens | 27 | 0 | 0 | 100% | Against Mac Mini port 5433 with 7918 foods + 38 unit conversions |
| Recipe analyze eval | 3 | 0 | 0 | 100% | Schema validation + structural checks |
| Coach insight eval | 3 | 0 | 0 | 100% | Structural + content quality checks |
| Food-parse HTTP eval | — | — | — | SKIPPED | Requires running dev server; not started this session |
| Router test suite | 14 | 0 | 0 | 100% | Policy resolution + pricing coverage |
| Enterprise invariants | 3 | 0 | 0 | 100% | Source-level static guards |

**Caveats:**

1. **Accuracy test DB fallback**: The accuracy test defaults to Supabase local (port 54322) when `DATABASE_URL` is unset. Earlier sessions hitting port 54322 saw 21/23 (91.3%) due to different unit conversion data. This session explicitly set `DATABASE_URL` to port 5433. The test should refuse to run without an explicit `DATABASE_URL` (tracked in TODO-NEXT.md).

2. **Eval suite coverage is thin**: 3 recipe + 3 coach insight cases. This is a smoke test, not a comprehensive eval. Edge cases (malformed input, very long recipes, non-English foods) are unmeasured.

### Unmeasured accuracy claims (❓)

| Claim | Status | Why unmeasured |
|-------|--------|----------------|
| Coach insight quality (nuance, actionability) | ❓ | 3 structural checks pass, but no human-judged quality rubric |
| Meal suggestion quality | ❓ | No eval suite exists. Deadline to build one: 2026-05-09 |
| Multilingual food parse (Greek/Spanish) | ❓ | Golden suite has Greek cases (`φέτα`, `σπανακόπιτα`) but limited. No Spanish food goldens |
| Photo food recognition accuracy | ❓ | No eval or test. Route exists, model routed via `pick('photo_analyze')` |
| Memory extraction quality | ❓ | Test suite exists (316 lines) but 0 rows in `memory_chunks` — never run against real user data |
| Form check pose accuracy | ❓ | MediaPipe integration exists; no automated test for angle accuracy |

---

## Honest Gap List (priority order)

1. **Production is 47 commits behind and missing server-side auth**
   - Severity: **MEDIUM** (was "Blocker for prod" — downgraded after data exposure check). `/dashboard` returns HTTP 200 with a purely structural skeleton (19KB of `animate-pulse` placeholder divs, SVG circles — zero user data, zero names, zero emails, zero IDs). SSR does NOT include any user-specific data. Data fetching goes through Supabase JS client-side, gated by RLS + valid JWT in `localStorage`. API routes `/api/profile`, `/api/food/log`, `/api/ai/food-parse` don't exist on production `main` (404). One route (`/api/ai/meal-suggest`) accepts anonymous POSTs (cost-abuse risk, not data risk). v0.3-overhaul's `proxy.ts` closes the shell leak for defense-in-depth.
   - Estimated work: L (schema migration + env var setup + Supabase cutover)
   - Why it matters: Dashboard layout shell is publicly accessible. No PII exposure, but poor security posture — v0.3-overhaul fixes this properly with server-side auth gate

2. **Voyage AI key is invalid**
   - Severity: **P1** — food embedding pipeline cannot run
   - Estimated work: S (rotate key, update `~/.local/secrets/voyage.env`)
   - Why it matters: Cannot re-embed foods or run the embed-foods ingest script. Confirmed: `curl -X POST api.voyageai.com/v1/embeddings` with the key from `~/.local/secrets/voyage.env` returns HTTP 401 "Provided API key is invalid." Key was rotated or expired since the last successful embed run.

3. **Cost tracking tables are empty — no observability**
   - Severity: **P1** — cannot answer "how much does Trophē cost to run?"
   - Estimated work: M — writer code exists in two places (`lib/api-cost-logger.ts` writes to `api_usage_log` via Supabase JS client; `agents/food-parse/index.v4.ts` writes to `agent_runs` via Drizzle). Neither path has been exercised: the Supabase writer targets production (not local DB), and the v4 food-parse writer has never run against real traffic. Wiring requires: ensure `agent_runs` table exists on production (via Phase 9 migration), then either route `logAPIUsage()` through Drizzle or keep the Supabase client path and verify it INSERTs.
   - Why it matters: Flying blind on cost. Budget estimates exist in code comments but aren't validated

4. **Langfuse traces not landing in DB**
   - Severity: **P2** — Langfuse server is healthy but app code doesn't appear to write traces
   - Estimated work: M (verify Langfuse SDK integration, check if env vars are loaded at runtime)
   - Why it matters: OTel GenAI semconv traces are printed to stdout in evals but don't persist

5. **Meal suggest has no eval suite**
   - Severity: **P2** — model upgrade blocked (deadline 2026-05-09)
   - Estimated work: S (15 prompts, ~30 min)
   - Why it matters: `gemini-2.0-flash` → `gemini-2.5-flash` upgrade is gated on this eval

6. **rls.test.ts fails locally as trophe_user**
   - Severity: **P2** — RLS tests only run in CI, not locally
   - Estimated work: S (add `RLS_TEST_DATABASE_URL` env var)
   - Why it matters: Developer can't verify RLS behavior locally

7. **Accuracy test silent DB fallback**
   - Severity: **P2** — test results depend on which DB the runner points at
   - Estimated work: S (require explicit `DATABASE_URL`, no fallback)
   - Why it matters: Gate passes or fails depending on env, with no warning

8. **Coach dashboard has no automated tests**
   - Severity: **P2** — 5,186 lines of UI with zero test coverage
   - Estimated work: L (E2E + unit tests for 9 pages)
   - Why it matters: Regressions in coach UI are invisible until a tester reports them

9. **OpenBrain credential audit across other repos**
   - Severity: **P2** — `brain_user` password still hardcoded in other repos
   - Estimated work: M (grep + edit pattern across 3-5 repos)
   - Why it matters: Trophē is clean but the credential itself is still exposed elsewhere

---

## What I Did NOT Verify (and why)

- **Production Supabase data**: No production DB credentials in this session. Cannot query production `food_log`, `profiles`, or cost tables
- **Spike webhook with real device**: No tester has connected a wearable device. 0 rows in `wearable_data`
- **Dev server runtime behavior**: Did not start `npm run dev`. Food-parse HTTP eval was skipped. Browser-based flows (trilingual toggle, coach UI interactions, client dashboard) not exercised
- **Production cutover dry run**: Out of scope for this diagnostic session
- **Langfuse trace ingestion**: Server is healthy, but didn't verify the SDK configuration or run an instrumented request to see if traces arrive
- **tRPC adoption breadth**: Confirmed the infrastructure exists; did not audit which coach pages use tRPC vs direct fetch
- **Form check MediaPipe accuracy**: Component exists, MediaPipe referenced, but no camera/pose test was run

---

## Recommended Next Session

**Focus: Rotate the Voyage AI key, then wire cost tracking + build meal_suggest eval.**

The Voyage key is confirmed invalid (HTTP 401). Supabase anon key is fine (sanity check corrected the original false alarm). Rotating Voyage is S-sized and unblocks the food embedding pipeline.

Pair the key rotation with cost-tracking wiring (gap #3) — the writer code already exists in two files but has never been exercised. That turns "no observability" into real numbers and is M-sized work with high leverage.

Then build the meal_suggest eval (deadline 2026-05-09, S-sized, 15 prompts). Three fixes in one session is realistic.

Production cutover (Phase 9) should wait until the Voyage key is rotated and the meal_suggest eval is complete. That's roughly 2 sessions of work.

---

## Corrections after sanity checks

Three claims from the initial audit were re-verified. Two held; one was wrong.

| Check | Original claim | Verification | Corrected? |
|-------|---------------|--------------|------------|
| **Voyage AI key** | ❌ Invalid (HTTP 401) | Re-tested with `curl -X POST api.voyageai.com/v1/embeddings` using key from `~/.local/secrets/voyage.env` → HTTP 401 "Provided API key is invalid." | No — original claim correct |
| **Supabase production 401** | ⚠️ REST returns 401, key may be rotated | Re-tested with **both** `apikey` and `Authorization: Bearer` headers (PostgREST requires both) → HTTP 200 with `[]`. Original test only sent `apikey` header. | **Yes** — key is valid. Changed from ⚠️ to ✅ in Integration Health table. Removed from gap list (was #3). |
| **Production /dashboard auth** | ❌ Returns 200 without login | `curl -sL` follows redirects → Final URL `/dashboard`, HTTP 200, 0 redirects. Response HTML contains full dashboard skeleton (macro circles, glass panels, progress bars). No `router.push('/login')`, no `<meta http-equiv="refresh">`, no client-side redirect in rendered HTML. | No — original claim correct. Dashboard shell renders without auth. |

**Net effect on gap list**: Reduced from 10 items to 9. Supabase key is healthy — one fewer P1.

### Production data exposure check (2026-05-02 follow-up)

- **Check A (HTML inspection)**: outcome **(a)** — SSR returns purely structural skeleton. 19KB of `animate-pulse` placeholder divs, SVG macro circles with hardcoded radii, glass panel containers. Zero user-identifying strings, zero emails, zero names, zero non-zero values. The `ClientPageRoot` component renders loading skeletons server-side; all data fetching happens client-side after hydration via Supabase JS.
- **Check B (API routes)**:
  - `/api/profile` → 404 (doesn't exist on production `main`)
  - `/api/food/log?date=2026-05-02` → 404 (doesn't exist on production `main`)
  - `/api/ai/food-parse` → 404 (doesn't exist on production `main`)
  - `/api/ai/meal-suggest` POST → 400 with validation error `{"error":"remaining_calories must be a non-negative number"}` — route exists, does input validation, but does **not** check auth. Anonymous users can call this route.
  - `/api/auth/signup` → 405 (exists, rejects GET)
  - Data fetching on production goes through Supabase JS client directly, gated by RLS + JWT in `localStorage`.
- **Verdict**: severity **MEDIUM**. Dashboard layout shell is publicly accessible (structural only, no PII). Data is protected by Supabase RLS (client-side JWT required). One AI route accepts anonymous POSTs (cost-abuse vector, not data leak). Gap #1 severity downgraded from "Blocker for prod" to "MEDIUM" — production cutover is important for defense-in-depth but not an emergency.

**Updated gap totals**: 9 items. 0 blockers, 2 P1, 7 P2 (gap #1 reclassified from blocker to P2 after data exposure check confirmed no PII risk).
