# Bug Hunt 2026-07-10 — ledger

## Checkpoint status (token-limit stop, mid-run)
Workflow `wf_b756fe49-c09`: all **13 finders completed** (59 unique candidates); adversarial verify was ~60% done (107/177 verdicts) when stopped to conserve tokens. **Resume** with the full script + `resumeFromRunId: wf_b756fe49-c09` (cached finder results return instantly).

### Fixed + shipped this checkpoint (branch fix/bug-hunt-2026-07-10)
- [x] **CRITICAL** RLS — profiles role self-escalation → migration 0058 `private.profiles_guard_role` trigger. **Applied to prod + proven live** (self-promote blocked, super_admins unchanged).
- [x] **HIGH** RLS — client_profiles coach_id self-enroll → migration 0058 `private.client_profiles_guard_coach` trigger. Applied to prod.
- [x] **HIGH** GDPR export queried client_habits + questionnaire_responses by non-existent `user_id` → fixed to `client_id` (verified against live schema; erasure engine was already correct).
- [x] **HIGH** GuidedSession finish volume + PR weights double-converted in lb mode → finish stats now stored in kg.
- [x] **HIGH** food-parse: peanut/peanut-butter misclassified as legume (substring 'pea') → reordered nut_seed before legume.
- [x] **HIGH** login page magic link used browser signInWithOtp (creates accounts, no rate limit/consent) → routed through guarded /api/auth/magic-link.
- [x] **HIGH** coach meal-plan-macros: no rate limit + unbounded LLM fan-out → added per-coach rate limit (20/10min) + MAX_UNIQUE_MEALS cap.

### Deferred (needs care / next pass on resume)
- [ ] **HIGH** food.search leading-wildcard ILIKE seq scan → add pg_trgm GIN index (safe additive migration; verify pg_trgm + exact column first).
- [ ] **HIGH/MED** food-parse index.v4 run() has no aggregate deadline (multi-item all-DB-miss > Vercel 60s) — the long-standing deferred bug; safe fix needs the local :5433 accuracy DB to verify the hot-path refactor.
- [ ] ~50 medium/low candidates below still pending final adversarial verdict (resume the workflow to score them).

---

## All candidates (raw, unverified below the fixed set above)

All 13 finders completed (59 unique candidates). Adversarial verify was ~60% done when the run was checkpoint-stopped to save tokens; resume via Workflow scriptPath + resumeFromRunId=wf_b756fe49-c09 (cached finder results return instantly). Triage/fix status tracked in commits on fix/bug-hunt-2026-07-10.

- [ ] **CRITICAL** `drizzle/0008_harden_rls_and_supabase_integration.sql:102` — Privilege escalation: any user can promote themselves to super_admin via profiles UPDATE
  - when: policy profiles_own_update is `FOR UPDATE USING (auth.uid()=id) WITH CHECK (auth.uid()=id)` with no restriction on which columns may change, and there is NO BEFORE UPDATE trigger on profiles (only audit_log and messages have column-immutability guards). profiles.role is a plain user_role enum column (0000/0001) whose values include 'super_admin'/'admin'. A logged-in client, using the browser anon client with their own JWT (lib/supabase/browser.ts → role 'authenticated'), runs `supabase.from('profiles').update({ role: 'super_admin' }).eq('id', myId)`. WITH CHECK passes (id=self), the enum CHECK passes, and the client is now super_admin — unlocking every requireSuperAdmin()/coachProcedure route, all clients' PHI, cost/audit data, and the GDPR erasure engine.
  - fix: Add a BEFORE UPDATE trigger (like private.messages_guard_client_update in 0053) that raises if NEW.role IS DISTINCT FROM OLD.role unless private.is_super_admin(); or split profiles into a self-updatable policy that excludes role and a separate super_admin-only policy for role changes.
- [ ] **HIGH** `components/workout/GuidedSession.tsx:551` — Guided finish-screen volume is double-converted in lb mode
  - when: User has unit=lb. They log Bench 100kg×5 (shown/stored as 220.5 lb display). totalVolume becomes 220.5*5 = 1102 (already lb). Line 551 does kgToDisplay(1102,'lb') = 1102/0.4536 ≈ 2430, shown as '2.4k lb'. True volume is ~500 kg / ~1102 lb, so the celebration shows ~2.2× the real number. (kg mode is a no-op so the bug is invisible there.)
  - fix: Store volume in kg: in handleFinish set weight: displayToKg(parseFloat(s.weight)||0, unit) so done.weight/totalVolume are kg, then the existing kgToDisplay at render is correct. Same fix feeds the PR list below.
- [ ] **HIGH** `components/workout/GuidedSession.tsx:578` — Guided finish-screen PR weights are double-converted in lb mode
  - when: unit=lb, user hits a PR bench at 102.5kg → shown/stored display 225.9 lb → pr.weight = 225.9. Line 578 renders kgToDisplay(225.9,'lb') = 225.9/0.4536 ≈ 498, so the gold PR line reads '498 lb' instead of ~226 lb. kg mode unaffected.
  - fix: Make done.weight kg (displayToKg on the parsed display string) so kgToDisplay(pr.weight, unit) at render produces the correct display value; or render pr.weight directly and stop the second conversion.
- [ ] **HIGH** `agents/food-parse/food-category-defaults.ts:102` — Peanuts misclassified as legumes → ~4.6x calorie / ~100x fat underestimate
  - when: classifyIngredient() iterates CATEGORY_KEYWORDS in array order; 'legume' (index 9, keyword 'pea') is checked before 'nut_seed' (index 10). For 'peanut butter' or 'peanuts', lower.includes('pea') matches first, so getCategoryMacros returns legume defaults {kcal:130, protein:9, carbs:22, fat:0.5} instead of nut_seed {kcal:600, protein:18, carbs:15, fat:52}. When a decomposed dish contains peanut/peanut-butter as an ingredient that misses the foods table, decompose.ts aggregates 130 kcal & 0.5g fat per 100g vs the true ~588 kcal & ~50g fat — a ~4.6x calorie and ~100x fat error in the meal total. Verified with a runtime repro: classify('peanut butter') -> 'legume'.
  - fix: Reorder so nut_seed precedes legume, OR make 'pea' a whole-word match (word-boundary regex) instead of substring so it doesn't hit 'peanut'. Also guard 'corn'→'popcorn' similarly.
- [ ] **HIGH** `app/api/coach/meal-plan-macros/route.ts:69` — Unbounded LLM fan-out with no rate limit on coach macro-rollup route
  - when: The route authenticates with requireRole('coach'...), which — unlike guardAiRoute used by the /api/ai/* routes — performs NO rate limiting. The meal_plan_entries query on line 55 has no .limit(), and mapPool on line 69 issues one full food-parse LLM (DeepSeek) call per DISTINCT meal description with zero cap on the number of unique descriptions. A coach (or a compromised coach account) can populate one of their own clients' meal plans with hundreds of distinct free-text descriptions, then POST { clientId } repeatedly. Each single HTTP request amplifies into hundreds of billed LLM calls, and there is no per-user request throttle to stop the loop → runaway LLM cost / DoS.
  - fix: Wrap the handler in consumeRateLimit(`meal-macros:${userId}`, N, window) like the /ai routes, and cap uniqueDescriptions.length (e.g. .slice(0, 60)) with a clear 'plan too large' response beyond that.
- [ ] **HIGH** `app/login/page.tsx:99` — Client-side magic link creates accounts, bypassing consent capture, the server magic-link route, and its rate limit
  - when: handleMagicLink calls the browser client supabase.auth.signInWithOtp({ email }) with no options. Supabase defaults shouldCreateUser to true, so submitting ANY email that is not yet registered provisions a brand-new Supabase auth user and emails a magic link. Clicking it logs the user in at /dashboard with NO Art.9 consent record, NO profiles row from the reservation flow, and NO role — an orphan account. This deliberately sidesteps app/api/auth/magic-link/route.ts (line 66 sets shouldCreateUser:false) and its 10/hour IP rate limit (line 30), and defeats the entire WP1 consent-gated signup that submitSignup enforces. A visitor can also trigger unlimited Supabase auth emails to an arbitrary victim address (mailbombing / enumeration), constrained only by Supabase's built-in project limits.
  - fix: Route the login-page magic link through POST /api/auth/magic-link (which already enforces shouldCreateUser:false, safe emailRedirectTo, and rate limiting) instead of calling the browser SDK directly; at minimum pass options: { shouldCreateUser: false } to signInWithOtp.
- [ ] **HIGH** `app/api/privacy/export/route.ts:25` — GDPR data export queries questionnaire_responses by a non-existent user_id column, silently dropping intake answers
  - when: A client who completed the intake questionnaire calls GET /api/privacy/export (Art. 20 self-service export). USER_TABLES maps questionnaire_responses to column 'user_id', but that table has NO user_id column — its subject key is client_id (drizzle/0027_intake_daily_checkins.sql line 26). PostgREST returns a 400 (column does not exist); the loop at line 40-43 catches error and writes data['questionnaire_responses'] = {error:'unavailable'}. The user's entire intake interview (sensitive personal data) is therefore OMITTED from the machine-readable export, while the export still returns HTTP 200 and claims completeness — a silent Art. 20 compliance gap.
  - fix: Change the questionnaire_responses entry to { table: 'questionnaire_responses', column: 'client_id' }. Add a test asserting each USER_TABLES column actually exists (the same Drizzle introspection the erasure-coverage test already uses).
- [ ] **HIGH** `app/api/privacy/export/route.ts:22` — GDPR data export queries client_habits by a non-existent user_id column, silently dropping habit history
  - when: Same export endpoint: USER_TABLES line 22 maps client_habits to column 'user_id', but client_habits has no user_id — the subject reference is client_id (drizzle/0000_complex_johnny_blaze.sql lines 107-121; the app itself always queries client_habits with .eq('client_id', ...)). The .eq('user_id', userId) select 400s, is caught, and data['client_habits'] becomes {error:'unavailable'}. Every client's assigned-habit history (streaks, completions, coach notes) is excluded from their Art. 20 export even though the endpoint reports success.
  - fix: Change the client_habits entry to column 'client_id'. Same schema-column existence test as above would have caught both this and the questionnaire_responses row.
- [ ] **HIGH** `drizzle/0008_harden_rls_and_supabase_integration.sql:109` — Client can self-enroll into any coach's tenant by rewriting client_profiles.coach_id
  - when: policy client_profiles_own_all is `FOR ALL USING (auth.uid()=user_id) WITH CHECK (auth.uid()=user_id)` — the only constraint is that the row is the caller's own. client_profiles.coach_id (and coach-managed fields like target_calories/goal) are therefore fully client-writable. A malicious client runs `supabase.from('client_profiles').update({ coach_id: victimCoachId }).eq('user_id', myId)`. Now private.is_coach_of(myId) returns true for the victim coach, so appt_client_insert (0028) accepts the client's bookings against that coach (coach_id matches), letting the attacker occupy/deny slots on the victim coach's calendar via uniq_appt_coach_slot, read the coach's availability/time_off, and message them — cross-tenant enrollment with no coach consent. The client can also overwrite coach-assigned macro targets on their own row.
  - fix: Restrict the client's own-row policy to SELECT plus an UPDATE that forbids changing coach_id (trigger or column-scoped policy); coach assignment should only be settable by the invite/activation flow (service role) or by private.is_coach_of/admin.
- [ ] **HIGH** `agents/food-parse/index.v4.ts:1338` — No aggregate deadline; DB-miss decompositions run serially → multi-item all-miss meals exceed the Vercel function timeout
  - when: A coach logs a 4-item meal of foods absent from the DB and not classified as composite (e.g. '300g natto, 200g tempeh, 150g seitan, 100g dulse'). Extraction ~10s, then 4 serial llmDecompose calls each up to 20s = ~80s of serial LLM work (before ingredient lookups). This blows past Vercel's 60s ceiling → the platform kills the function → 504/500 and the ENTIRE meal is lost, not just the slow item. Even 3 slow all-miss items breach a 60s limit; with no maxDuration the account default is lower still.
  - fix: Introduce an overall deadline (e.g. AbortController armed at ~45s at run() entry, propagated into executeAiTask). Convert the serial DB-miss decompose (line 1338) into a single Promise.all over all miss indices (like Step 2b at line 1093). Add `export const maxDuration` to route.ts and, when the deadline trips, return partial results + a warning instead of failing the whole parse.
- [ ] **HIGH** `lib/trpc/routers/food.ts:544` — food.search does a full sequential scan of the foods table on every autocomplete keystroke
  - when: The query builds q = `%${input.query}%` (line 526) and runs `.where(ilike(foods.nameEn, q))`. A leading-wildcard ILIKE cannot use a btree index, and the only text index on `foods` is a GIN index on the generated `search_text` tsvector (drizzle/0004 + 0022), which ILIKE does not use. There is no pg_trgm/gin_trgm_ops index anywhere on `foods` (confirmed: grep for trgm/name_en indexes returns nothing). So a coach typing 'chicken' into the food-log autocomplete triggers Postgres to sequentially scan the entire foods table (~43K rows today, and the schema header at foods.ts:8 anticipates 50–300k OFF rows) for each debounced request. At 300k rows this is tens-to-hundreds of ms of DB CPU per keystroke, serialized behind the tRPC round-trip, despite the comment claiming it is the 'fast' lane.
  - fix: Add a GIN trigram index (`CREATE INDEX ... ON foods USING gin (name_en gin_trgm_ops)` after `CREATE EXTENSION pg_trgm`) so ILIKE '%q%' is index-backed, or rewrite the endpoint to use the existing search_text tsvector GIN index via `to_tsquery`/websearch_to_tsquery with a prefix match instead of ILIKE.
- [ ] **HIGH** `app/api/privacy/export/route.ts:22` — GDPR Art.20 export queries client_habits and questionnaire_responses by non-existent user_id column → silently omits the subject's habits + intake answers
  - when: USER_TABLES lists `{ table: 'client_habits', column: 'user_id' }` (line 22) and `{ table: 'questionnaire_responses', column: 'user_id' }` (line 25). Neither table has a user_id column — client_habits keys on client_id (db/schema/habits.ts:57) and questionnaire_responses keys on client_id (drizzle/0027_intake_daily_checkins.sql:26). So `service.from('client_habits').select('*').eq('user_id', userId)` returns a PostgREST error ('column client_habits.user_id does not exist'). Line 43 swallows it: `data[table] = error ? { error: 'unavailable' } : (rows ?? [])`. Concrete failure: a client (or clinic on their behalf) downloads their Art.20 export; the JSON contains `"client_habits": {"error":"unavailable"}` and `"questionnaire_responses": {"error":"unavailable"}` instead of their assigned habits and intake questionnaire answers (Art.9 health data). The portability deliverable silently drops two categories of the subject's personal data, and the silent-error tolerance hides the defect. This shipped as WP5 (remediation ledger claims export 'execution' done).
  - fix: Change both entries to `column: 'client_id'`. Better: fail loud on unexpected PostgREST errors (distinguish 'relation missing' from 'column missing') instead of blanket `{ error: 'unavailable' }`, so a wrong-column mapping reds a test rather than shipping a lossy legal export. Add a portability-coverage test mirroring erasure-coverage.test.ts.
- [ ] **MEDIUM** `lib/trpc/routers/food.ts:544` — food.search only matches English names, ignoring Greek/Spanish columns and the GIN index
  - when: A Greek coach/client types a Greek dish name (e.g. "Χωριάτικη" or "κοτόπουλο") into the food-log autocomplete. `ilike(name_en, '%κοτόπουλο%')` matches no English column, so the endpoint returns an empty array even though the food exists under name_el. Every non-English query returns zero results; each query also seq-scans 43K rows instead of using the GIN index.
  - fix: Search across the multilingual expression: use the tsvector/search_text GIN index (e.g. `to_tsvector(...) @@ plainto_tsquery(...)`) or at minimum `or(ilike(nameEn,q), ilike(nameEl,q), ilike(nameEs,q))`, ideally with a trigram/GIN-backed predicate so the index is usable.
- [ ] **MEDIUM** `lib/trpc/routers/clients.ts:56` — clients.list returns total = rows.length (page size) instead of the true count
  - when: A coach with 200 assigned clients calls clients.list with the default limit 50. The query returns 50 rows and `total: 50`. Any pagination UI computing page count from `total` concludes there is exactly one page and never lets the coach reach clients 51–200. On the last partial page `total` also shrinks (e.g. 30) making the count actively misleading.
  - fix: Run a separate `SELECT count(*)` over the same `clientAccessPredicate(coachId, role)` join (without limit/offset) and return that as `total`, or use a window `count(*) OVER ()` in the main query.
- [ ] **MEDIUM** `lib/trpc/routers/food.ts:279` — food.log.add buckets entries by UTC date, misattributing late-night logs for non-UTC users
  - when: A user in Greece (UTC+2/+3) logs a meal at 01:00 local on 2026-07-10, which is 22:00 UTC on 2026-07-09. With loggedAt omitted, the server stores loggedDate = '2026-07-09'. The client's food.log.list query for '2026-07-10' (today) does not return the entry, and the daily summary attributes those calories/macros to the previous day — corrupting adherence and totals for every late-night log.
  - fix: Compute loggedDate in the user's timezone (pass an IANA tz or a client-computed local YYYY-MM-DD), or accept an explicit `loggedDate` from the client instead of slicing a UTC ISO string.
- [ ] **MEDIUM** `lib/trpc/routers/memory.ts:37` — memory access guard ignores admin/super_admin org access, only allowing the directly-assigned coach
  - when: A super_admin (or an org admin) opens a client's memory card in the super-admin command center and calls memory.stats/memory.list for that client's userId. Because they are not the row's coachId, the assignment lookup returns nothing and the call throws FORBIDDEN — even though they can access that same client through every clients.*/coach.*/food.coachEdit endpoint. Legitimate admin/super-admin memory views are wrongly blocked.
  - fix: Reuse canAccessClient(ctx.db, requesterId, role, targetUserId) (which honors super_admin=true and admin org membership) instead of the bespoke coachId-only lookup.
- [ ] **MEDIUM** `components/workout/workout-persistence.ts:126` — Ghost-set query has no ORDER BY under a 600-row cap, so 'last session' ghosts can be wrong for heavy users
  - when: A 2-year user with a 6-exercise push template (≈4 sets × ~150 sessions ≈ 3600 rows for those exercises) opens guided mode. The unordered limit(600) returns an arbitrary/early physical slice; latestDate is computed as some old date, so every ghost weight/rep placeholder shows stale values from months ago instead of last week's session.
  - fix: Add .order('created_at', { ascending: false }) (or order by the joined session_date desc) before limit, or fetch per-exercise with a small ordered limit; ensure the most-recent session is guaranteed present.
- [ ] **MEDIUM** `agents/food-parse/enrich.ts:11` — Substring food matching returns wrong macros (eggplant→egg, pineapple→apple)
  - when: enrichWithLocalDB matches with `nameLower.includes(n.split(',')[0].trim())`. The first token of 'Egg, whole' is 'egg' and of 'Apple' is 'apple'. Logging 'eggplant' (DB-miss legacy path) makes 'eggplant'.includes('egg')=true → item is enriched with Egg macros (143 kcal, 12.6g protein, 9.5g fat per 100g) instead of eggplant (~25 kcal). Likewise 'pineapple'.includes('apple')=true → pineapple gets Apple macros (52 kcal). Verified: both includes() return true. Result: user's logged food shows grossly wrong calories/macros.
  - fix: Require whole-word/token match rather than raw substring containment (split names into tokens and compare, or use word-boundary regex), and pick the longest/most-specific match instead of the first FOOD_DATABASE hit.
- [ ] **MEDIUM** `lib/food/calorie-equations.ts:54` — macroSplit returns a calorie total that exceeds the goal-adjusted target for heavy clients
  - when: macroSplit anchors protein to 2.0 g/kg of current bodyweight and fat to 25% of calories, then fills carbs with `Math.max(0, remaining/4)`. For a heavy client on a deficit — e.g. macroSplit(1200, 150) — protein_g=300 (1200 kcal) and fat_g=33 (297 kcal) alone total 1497 kcal; remaining is negative so carbs clamp to 0, and the returned `calories` is recomputed as 300*4+0+33*9 = 1497. computeBaseline (used by /api/coach/client-tdee) therefore returns target.calories=1497 for a client the coach put on a 1200-kcal target — silently erasing the deficit by ~25% and showing 0g carbs.
  - fix: When protein*4+fat*9 exceeds the calorie target, scale protein (and/or fat) down to fit the target, or cap protein at a lean-mass/goal-weight basis, so the recomputed calories never exceed the requested target.
- [ ] **MEDIUM** `lib/food/nutrition-engine.ts:89` — calculateMacroTargets macros can imply more energy than the returned calorie target
  - when: protein_g and fat_g are fixed g/kg (e.g. fat_loss: 2.2 protein + 0.8 fat), carbs fill the remainder via `Math.max(0, targetCalories - proteinCalories - fatCalories)/4`. For a heavy fat_loss client (e.g. 150 kg): protein 330g (1320 kcal) + fat 120g (1080 kcal) = 2400 kcal already exceeds the ~2300 kcal deficit target, so carbs_g=0 and the returned macro block (protein_g*4+fat_g*9 = 2400) is inconsistent with the returned `calories` field (2303). /api/nutrition/calculate returns both, so clients see macros that don't sum to the stated calories.
  - fix: After computing protein/fat, if their Atwater sum exceeds targetCalories, reduce fat (then protein) proportionally so protein_g*4+carbs_g*4+fat_g*9 ≈ calories; never leave carbs clamped-to-0 with an over-budget protein+fat.
- [ ] **MEDIUM** `app/dashboard/workout/stats/page.tsx:121` — getWeekStart mixes local getDay/getDate with UTC toISOString → workouts bucketed into wrong week
  - when: getWeekStart parses `new Date(session_date)` where session_date is a plain 'YYYY-MM-DD' (interpreted as UTC midnight), then reads d.getDay()/d.getDate() in the browser's LOCAL timezone and finally emits monday.toISOString().slice(0,10) (UTC). For users in negative-UTC offsets, UTC-midnight of a Monday is the prior Sunday evening locally, so getDay() returns the previous day and the Monday-based `diff` is off by one — a workout logged Monday can be assigned to the previous week. This corrupts weeklyTrend (last-8-weeks bars) and the `>= thisWeekStart` this-week volume filter for all Americas users.
  - fix: Compute week start using local date parts consistently: parse the date as local (split the string) and format the Monday with local getFullYear/getMonth/getDate (like lib/utils/dates.localDateStr) instead of toISOString().
- [ ] **MEDIUM** `app/api/coach/shopping-list/route.ts:30` — Coach LLM shopping-list route has no rate limiting
  - when: Route is gated only by requireRole('coach','admin','super_admin') (line 30), which does not rate-limit. Every POST triggers a DeepSeek structured-extraction call (executeAiTask at line 66). The comparable AI endpoints (/api/food/parse, /api/ai/*) all route through guardAiRoute's 60-req/15-min limiter; this one does not, so an authenticated coach can call it in a tight loop to drive unbounded LLM spend (each call bounded only by their own clients' meal count).
  - fix: Add consumeRateLimit(`shopping-list:${userId}`, ...) before the executeAiTask call, mirroring guardAiRoute's window/limit.
- [ ] **MEDIUM** `middleware.ts:76` — Redirect branches discard the session cookies refreshed by getUser(), causing intermittent logout under refresh-token rotation
  - when: createSupabaseMiddlewareClient writes refreshed auth cookies onto the `response` object (lib/supabase/middleware.ts setAll). When an authenticated user with an expired access token but valid refresh token hits /login or /signup, getUser() (line 50) silently rotates the tokens and stages the new Set-Cookie on `response`. But line 77 returns a fresh NextResponse.redirect(/dashboard) that does NOT carry those cookies. With Supabase refresh-token rotation (default on), the old refresh token is now consumed/invalid, yet the browser still holds it. The next request to /dashboard fails to refresh → user is treated as anonymous → bounced back to /login, effectively logged out. The protected→/login branch (line 71) has the same cookie-dropping defect.
  - fix: Copy the staged cookies onto the redirect response before returning, e.g. const redirect = NextResponse.redirect(url); response.cookies.getAll().forEach(c => redirect.cookies.set(c)); return redirect; — for both redirect branches.
- [ ] **MEDIUM** `components/workout/GuidedSession.tsx:551` — Finish-screen volume and PR weights double-convert units for lb users
  - when: A user with weight unit set to 'lb' completes a workout: logs 3 sets of 100 lb × 10. The finish celebration shows total volume of ~6614 (should be 3000) and a PR badge reading '220 lb × 10' instead of '100 lb × 10'. Stored data in workout_sets.weight_kg is correct; only the celebration numbers are wrong. kg users see correct values, masking the bug.
  - fix: finishStats already holds display-unit numbers, so render them directly with the unit suffix instead of re-converting: replace `kgToDisplay(finishStats.totalVolume, unit)` with `finishStats.totalVolume` (line 551) and `kgToDisplay(pr.weight, unit)` with `pr.weight` (line 578). Alternatively, store kg in finishStats (convert set.weight back with displayToKg when building `done`) and keep the single kgToDisplay at render time.
- [ ] **MEDIUM** `components/shared/ChatThread.tsx:218` — Chat thread loads the OLDEST 200 messages (ascending + limit) so long threads never show recent messages
  - when: load() (line 213-219) and the 8s poll (line 250-256) both run .order('created_at', { ascending: true }).limit(200) on messages for a coach↔client pair. Once a coaching relationship exceeds 200 messages (realistic over months of daily chat with photos/voice notes), this returns the FIRST 200 messages ever sent, not the latest. On every page load and on every poll cycle the viewer sees ancient history and NONE of the recent conversation; new messages are only visible transiently via the realtime INSERT append during an already-open session, and disappear again on refresh. Read receipts (the separate UPDATE at line 225) still fire, so unread badges clear for messages the coach can't actually see.
  - fix: Fetch the newest 200 with .order('created_at', { ascending: false }).limit(200) then reverse client-side for display (or paginate with a keyset on created_at). Apply the same change to the polling query.
- [ ] **MEDIUM** `drizzle/0028_booking.sql:69` — appt_client_cancel lets a client mutate any appointment column while cancelling (evade late-cancel flag)
  - when: policy appt_client_cancel is `FOR UPDATE USING (client_id=auth.uid()) WITH CHECK (client_id=auth.uid() AND status='cancelled' AND cancelled_by='client')`. WITH CHECK constrains only status/cancelled_by, not the other columns, and there is no column-immutability trigger. A client cancelling within the 24h window runs an update that also sets `late_cancellation=false` (dodging the late-cancellation penalty the coach relies on) and can rewrite starts_at/duration_min/kind/note on the coach's appointment row in the same statement, as long as the final status is 'cancelled'.
  - fix: Add a BEFORE UPDATE trigger freezing all columns except status/cancelled_by/cancelled_at when the actor is the client, matching the messages_guard_client_update pattern in 0053.
- [ ] **MEDIUM** `agents/food-parse/decompose.ts:418` — decomposeAndLookup looks up ingredients serially instead of using the existing lookupFoodBatch
  - when: A 7-ingredient dish decomposition issues 7 sequential DB round-trips (~300-600ms each incl. BM25 + fuzzy + alias + canonical queries) ≈ 2-4s of pure serial DB latency per decompose, versus ~1 round-trip if batched. For an all-miss meal that runs decompose per item, this compounds and pushes total time toward/over the function timeout.
  - fix: Replace the loop with `const lookupResults = await lookupFoodBatch(lookupInputs);` — identical result shape, parallelized.
- [ ] **MEDIUM** `agents/food-parse/decompose.ts:266` — Partial (category-default) decompositions are cached at a hardcoded 0.75 confidence and re-served as source 'local_db'
  - when: First parse of 'sudado de X' matches only 2 of 5 ingredients (3 use category-average macros) → returned at confidence 0.45, source 'local_db+category_default'. It is written to dish_recipes at confidence 0.75. The second identical request now returns confidence 0.75, source 'local_db' — a mostly-guessed macro total is surfaced to coaches as authoritative verified-DB data, and the low-confidence UI warnings never fire.
  - fix: Persist the computed confidence and a truthful source (e.g. 'llm_decomp_partial') into the cache row instead of the constant 0.75/'llm_decomp'; or skip caching when matchRatio < 1 (or < some higher bar). Have lookupCachedRecipe* preserve the stored source rather than overwriting to 'local_db'.
- [ ] **MEDIUM** `agents/food-parse/lookup.ts:849` — COMMON_PIECE_WEIGHTS fuzzy match mis-weights short food names that are substrings of a longer piece-key
  - when: '1 piece of ham' — a foods row with nameEn 'Ham', no canonicalFoodKey, unit normalized to 'piece', no food-specific/default piece conversion → the loop hits 'hamburger'.includes('ham') === true and returns 150g (a whole hamburger's mass) instead of a ~15-30g ham slice, ~5-10x the real calories/fat. Same class: 'pea' → matches 'peach' (150g), etc. Grams drives all macros, so the whole item is corrupted.
  - fix: Require the pattern-side match to be word/token-bounded (split canonical key and weight key on '_' and compare whole tokens), or drop the `pattern.includes(strippedCk)` direction and keep only longest-token matching. At minimum require strippedCk length >= 4 and a token boundary before accepting.
- [ ] **MEDIUM** `app/api/food/local-search/route.ts:36` — Public local-search endpoint runs a triple leading-wildcard OR-ILIKE sequential scan on food_database
  - when: `.or(`name.ilike.%${query}%,name_el.ilike.%${query}%,name_es.ilike.%${query}%`)` (line 36) uses three leading-wildcard ILIKEs OR'd together. food_database has btree indexes on name/name_el/name_es (idx_food_db_name*) and a GIN tsvector index (idx_food_db_search), but none are usable by leading-wildcard ILIKE — so each request sequentially scans food_database and evaluates three ILIKE predicates per row. Because this is an unauthenticated public endpoint (rate-limited to 120 req/hr/IP), a handful of distinct IPs issuing normal search traffic each pin a DB core on repeated full scans, and the `ORDER BY popularity` forces a sort of the full match set on top of the scan.
  - fix: Replace the OR-ILIKE with a websearch/to_tsquery query against the existing idx_food_db_search GIN tsvector index, or add pg_trgm gin_trgm_ops indexes on name/name_el/name_es so the ILIKEs become index-backed.
- [ ] **MEDIUM** `app/coach/page.tsx:529` — Coach dashboard runs the last three data queries serially instead of in parallel
  - when: In loadClients, after the batched Promise.all at line 462, three mutually-independent Supabase queries are each awaited one after another: weekCheckins (line 529), habitsForAssign (line 549), and notesCount (line 564). None depends on another's result, yet they execute as a sequential waterfall, adding ~3 full client→Supabase round-trips (each ~50–150ms) to every coach dashboard load. The earlier `appts` (line 435) and `clientProfiles` (line 449) fetches are likewise independent but awaited serially, adding another avoidable round-trip before the parallel batch even starts.
  - fix: Move the independent tail queries (weekCheckins, habitsForAssign, notesCount) into a single Promise.all, and fold the independent `appts` fetch into the first parallel batch so only genuinely dependent queries remain sequential.
- [ ] **MEDIUM** `lib/locales/fr.ts:587` — 64 workout-UI i18n keys missing from all 5 overlay locales (fr/de/it/pt/nl) → English leaks
  - when: A user sets language to fr/de/it/pt/nl and opens the workout module. The premium workout keys added later (workout.picker_title, workout.muscle_chest..muscle_cardio, workout.cardio_run/cycle/swim/row/hiit/walk/other, workout.plate_title/plate_per_side/plate_bar, workout.superset*, workout.info_title/info_cue/info_pr, workout.no_workouts, workout.compound, etc. — 64 keys) exist inline (en/es/el) but are absent from every overlay file (each has 884 of 948 keys). t() falls back to English, so a French user sees the exercise picker, cardio picker, muscle-group chips, plate calculator and superset controls entirely in English. Verified rendered: workout.plate_title in components/workout/PlateCalculator.tsx, workout.info_title in components/workout/ExercisePicker.tsx, workout.superset/no_workouts in app/dashboard/workout/page.tsx.
  - fix: Add the 64 missing workout.* keys to fr/de/it/pt/nl overlay files (translated), or add a CI test that asserts every inline key exists in each overlay so future additions cannot silently regress to English.
- [ ] **MEDIUM** `lib/i18n.tsx:483` — ⭐ emoji baked into recs.coach_pick translation, rendered as the 'Coach Pick' category label
  - when: On the client food-recommendations screen, RecCard shows the category label via useCategoryLabel('coach_pick', t) → t('recs.coach_pick') which is '⭐ Coach Pick' / '⭐ Pick del Coach' / '⭐ Επιλογή Coach' (components/food/CoachFoodRecs.tsx lines 163 and 233). The ⭐ emoji is thus rendered in deployed client UI in all three core languages (and any overlay that copied it), violating the house rule NO emoji in deployed UI.
  - fix: Remove the ⭐ from all recs.coach_pick values in lib/i18n.tsx (and overlays); if a marker is desired, render a themed <Icon>/SVG star badge in CoachFoodRecs instead of embedding an emoji in the string.
- [ ] **MEDIUM** `components/habits/HabitDetailModal.tsx:88` — Mood face emoji (😄😊😐😓😰) rendered in coach habit-history modal
  - when: MOOD_EMOJI maps mood → 😄/😊/😐/😓/😰 and is rendered directly as <span>{MOOD_EMOJI[day.mood]}</span> at lines 212 and 232 in the coach-facing habit detail modal. Whenever a client logged a mood, the coach sees raw emoji faces in the deployed UI, violating the NO-emoji house rule (mood is already i18n'd elsewhere via mood.* keys, so the emoji is gratuitous).
  - fix: Replace MOOD_EMOJI rendering with the existing mood.* text labels or a small SVG/Icon mood glyph set; drop the emoji map.
- [ ] **MEDIUM** `app/demo/page.tsx:223` — Public /demo route littered with rendered emoji (flag emoji, traffic-light signals, feature chips)
  - when: The public /demo marketing page renders emoji as literal JSX text: the language toggle buttons show 🇬🇧 EN and 🇬🇷 EL (lines 223, 229); feature copy renders 🟢 🟡 🔴 signals (lines 134, 169, 429); input chips render 📸/🎤/📋 (lines 384-386); habit chips render 🐢 Eat Slowly, 🥩 Protein, 🥦 Vegetables, 💧 Hydration, 😴 Sleep, 🧘 Mindful (line 417). All are shown in the deployed UI, and the flag emoji specifically also violate the CLAUDE.md rule to use flagcdn.com images rather than emoji flags.
  - fix: Replace emoji with the app's Icon/SVG set and flagcdn.com <img> flags on the demo page; strip inline emoji from the demo content strings.
- [ ] **MEDIUM** `app/api/privacy/export/route.ts:14` — Art.20 export omits multiple PHI tables the erasure module itself tracks (supplement_log, wearable_data/connections, memory_chunks, raw_captures, appointments)
  - when: USER_TABLES + the ad-hoc messages/workout_sets queries cover ~16 tables, but lib/privacy/erasure.ts (CASCADE_COVERED / PRE_ERASURE_STEPS) enumerates user-keyed data that the export never reads: supplement_log (user_id), client_supplements (user_id), wearable_data (user_id), wearable_connections (user_id), memory_chunks (user_id), raw_captures (user_id), appointments (client_id), plus chat-attachment storage objects. Concrete failure: a client using supplement tracking + a wearable + booked appointments requests their data; the export returns 200 with a complete-looking payload but their supplement logs, wearable health data, and appointment history are absent. For the stated clinic-procurement posture this is an incomplete Art.20 response with no warning to the subject. The two modules have diverged: erasure was made schema-exhaustive (coverage test) while export was hand-maintained.
  - fix: Derive the export table list from the same schema-introspection source of truth used by erasure-coverage, or add the missing user-keyed tables (supplement_log, client_supplements, wearable_data, wearable_connections, memory_chunks, raw_captures, appointments) and the chat-attachment storage listing. Add a test asserting every table erasure classifies as user data is either exported or explicitly excluded-with-reason.
- [ ] **MEDIUM** `agents/food-parse/index.v4.ts:1338` — Deferred bug #13 still open: parse run() has no aggregate deadline and the DB-miss decompose fallback runs serially per item → multi-item all-miss meal can exceed the 60s function cap (504)
  - when: The Step-3 item loop (lines 1138–1356) calls `await decomposeAndLookup(...)` serially in the DB-miss branch (line 1338); decomposeAndLookup issues an LLM call (llmDecompose → executeAiTask, food_parse policy timeoutMs=20_000, agents/router/policies.ts:72). The Step-2 recipe-cache probe (lines 1069–1085) is also a serial per-item await. There is no overall run() budget. Concrete failure: a client types a 3-item meal of uncommon foods that all miss the recipe cache, miss lookupFoodBatch, and miss the static enrich DB (e.g. three obscure regional dishes not classified 'composite'): extraction LLM (~20s) + 3 serial decompose LLM calls (~20s each) = ~80s > vercel.json maxDuration:60 → the function is killed and the user gets a raw 504 (the client's 45s abort only masks it when it fires first). The ledger deferred this because the safe fix needs the local accuracy DB to verify; it remains unshipped in the current tree.
  - fix: Parallelize the Step-3 DB-miss decompose fallback (collect miss indices, `Promise.all` the decomposeAndLookup calls, as Step 2b already does for composites) and the Step-2 recipe-cache probes; add a wall-clock budget to run() that short-circuits remaining per-item LLM work and returns a partial/clarification result before the 60s cap. Verify against the accuracy suite alongside a local canonical-foods DB refresh.
- [ ] **MEDIUM** `app/api/ai/conversation/route.ts:49` — Conversation route has no error handling — AI/budget failure leaves an orphaned user message and returns a raw 500
  - when: An org that has hit its monthly AI budget (or during any DeepSeek outage) sends a chat message. Line 45 persists the user turn, then executeAiTask() throws at line 49. The route has no catch, so Next.js returns an opaque 500 to the client, the assistant reply is never written (line 68 skipped), and memory.markRetrieved() / enqueueConversationMemory never run — leaving a permanent orphaned user message with no response in the thread and a leaked internal error instead of the friendly retry copy the other routes return.
  - fix: Wrap the readMemory→executeAiTask→insert sequence in try/catch mirroring meal-suggest: on failure return a 502 with a stable code/message, and either insert the user message only after a successful generation or delete/mark it on failure so the thread isn't left with a dangling user turn.
- [ ] **LOW** `lib/trpc/routers/memory.ts:119` — memory.delete uses coachProcedure, making its self-deletion branch unreachable for regular users
  - when: A regular client who can see their own memory chunks (via memory.list) tries to delete one of their own chunks (calling memory.delete with chunkId and their own userId). coachProcedure throws FORBIDDEN ('Coach or admin role required') because the client lacks a coach role, so users can never remove their own memory entries despite the guard being written to allow exactly that.
  - fix: Make memory.delete a protectedProcedure and rely on assertCanReadMemory (updated per the finding above) for authorization, so self-deletion and coach/admin deletion both work.
- [ ] **LOW** `components/workout/GuidedSession.tsx:457` — Guided finish stats count warm-up sets in volume and set count
  - when: User completes 2 warm-up sets (40kg×10, 60kg×10) plus working sets. The finish card's Volume tile and Sets tile include 1000kg and 2 sets of warm-up, inflating both numbers versus the freestyle summary's convention.
  - fix: Add !s.is_warmup to the filter at line 457 (and optionally keep counting all sets for a separate 'total sets' metric if desired), matching the freestyle summary.
- [ ] **LOW** `app/dashboard/workout/page.tsx:1438` — Freestyle completion summary hard-codes 'kg' label, ignoring the user's lb preference
  - when: unit=lb user finishes a session; the big volume number in the celebration modal reads e.g. '5000 kg' even though the entire rest of the app shows their weights in lb, which is confusing/incorrect for their preference.
  - fix: Render kgToDisplay(finishSummary.volume, unit) with the {unit} suffix (guard the >1000 k-formatting), keeping storage in kg but displaying in the preferred unit.
- [ ] **LOW** `app/dashboard/workout/page.tsx:419` — superset_group on already-inserted sets goes stale when the link is toggled mid-session
  - when: User adds A and B, checks A set 1 (unlinked → superset_group=null persisted), then taps the Link2 button to superset A↔B, then checks A set 2 (→ superset_group=1 persisted). A's two sets in the same session now carry null and 1, so any superset grouping/analytics reads a single exercise as partially paired.
  - fix: On toggleSupersetLink, recompute and UPDATE superset_group for all already-inserted (dbId != null) sets of the affected chain, or defer superset_group assignment to the finish UPDATE for the whole session.
- [ ] **LOW** `app/dashboard/workout/page.tsx:1083` — Superset badge letter is derived from list index, so multiple supersets skip letters
  - when: User supersets exercises at positions 0-1 (group 1 → 'Superset A') and separately positions 3-4 (group 4 → 'Superset D'). The UI shows 'Superset A' and 'Superset D', skipping B and C, which looks like a bug to the user.
  - fix: Map distinct group ids to sequential ordinals for display (e.g. index into a sorted unique-groups list) so labels read A, B, C…, while keeping the positional id for the DB.
- [ ] **LOW** `components/charts/WeeklyMacroChart.tsx:125` — getBarColor divides by targetCalories with no zero guard
  - when: getBarColor computes `ratio = calories / targetCalories`. The parent passes `clientProfile?.target_calories ?? 2000`, but `??` only substitutes null/undefined — a client whose target_calories is stored as 0 keeps 0, so for any day with calories>0 the ratio is Infinity and every bar renders 'over' (red). targetY = chartHeight - (0/maxCal)*chartHeight also pins the target line to the axis.
  - fix: Guard: `const ratio = targetCalories > 0 ? calories / targetCalories : 0;` and treat targetCalories<=0 as 'no target set' in the color/threshold logic.
- [ ] **LOW** `app/api/food/local-search/route.ts:31` — Non-numeric limit param defeats the anti-scrape clamp and leaks raw DB error on a public endpoint
  - when: GET /api/food/local-search?q=egg&limit=abc → parseInt('abc') = NaN (line 13). The clamp on line 31, Math.min(Math.max(1, NaN), 50), evaluates to NaN (Math.max(1,NaN)===NaN), so safeLim is NaN and .limit(NaN) sends limit=NaN to PostgREST, which returns a 400 'unable to parse limit' error. That truthy error hits line 43, which returns the raw PostgREST/DB error.message to the unauthenticated caller with HTTP 500 — an information leak plus wrong status. The clamp intended to bound results at 50 is bypassed entirely by any non-numeric limit.
  - fix: const parsed = parseInt(...); const limit = Number.isFinite(parsed) ? parsed : 15; then clamp. On DB error, return a generic message instead of error.message.
- [ ] **LOW** `app/api/ai/conversation/route.ts:26` — Malformed request body throws instead of returning 400
  - when: Line 26 does `requestSchema.safeParse(await request.json())` with no try/catch and no `.catch(() => ({}))` (guardAiRoute runs before this). POSTing an empty body or invalid JSON to /api/ai/conversation makes request.json() throw a SyntaxError that is never caught, so the route returns a generic 500 instead of the intended 400 'Invalid conversation request'. Other routes in the codebase (e.g. barcode, invite-client) guard this with `.catch(() => ({}))`; this route is inconsistent.
  - fix: Use `await request.json().catch(() => ({}))` so safeParse yields a clean 400.
- [ ] **LOW** `app/api/ai/coach-insight/route.ts:38` — Uncaught authorization throw returns 500 instead of 403 (plus unguarded JSON parse)
  - when: assertCanAccessClient (line 38) throws a TRPCError('FORBIDDEN') when a coach requests a clientId outside their org/ownership, but this route handler has no try/catch, so the throw surfaces as a generic HTTP 500 rather than the intended 403 — a coach probing other clients' insight endpoint gets 500s. Separately, line 29 `await request.json()` is also unguarded, so a malformed/empty body throws → 500 instead of the intended 400.
  - fix: Wrap the handler body in try/catch that maps TRPCError FORBIDDEN → 403, or replace assertCanAccessClient with the boolean canAccessClient + explicit 403; and add `.catch(() => ({}))` to request.json().
- [ ] **LOW** `app/api/integrations/spike/callback/route.ts:68` — Spike callback returns unauthenticated users to /login?redirect=... but the login page only honors redirectTo
  - when: When the Spike OAuth callback runs with an expired/absent session it redirects to `${appUrl}/login?redirect=/dashboard/integrations`. The login page (app/login/page.tsx:22) reads only searchParams.get('redirectTo'), never 'redirect'. After the user logs in, they are routed to /dashboard (or /coach) by role instead of being returned to the integrations flow they started, so the wearable-connect journey silently drops.
  - fix: Use the same param name the login page reads: redirect to `/login?redirectTo=%2Fdashboard%2Fintegrations` (URL-encoded).
- [ ] **LOW** `app/login/page.tsx:22` — Login page never displays the ?error= that the auth callback sets on failure
  - when: app/api/auth/callback/route.ts redirects failures to /login with ?error=<message> (lines 31 and 41), e.g. after a failed PKCE code exchange or an OAuth provider error. LoginForm reads searchParams for redirectTo, code, mode, and confirmed, but never 'error', so the message is silently swallowed. A user whose magic-link/OAuth exchange fails lands on a blank login form with no explanation and no indication their link expired.
  - fix: Read searchParams.get('error') in the effect and surface it via setError so callback failures are shown to the user.
- [ ] **LOW** `components/shared/ChatThread.tsx:369` — Object URL for sent photo/voice attachment is never revoked
  - when: A coach in a long chat session sends 30 photos over an hour without reloading. Each createObjectURL blob (a full ~1600px JPEG, hundreds of KB) stays resident in the tab because it is never revoked, steadily growing memory on a mobile device.
  - fix: Track sent previewUrls and revoke them when the confirmed DB row arrives (e.g. in the send() success branch after the signed URL would be used, or when a poll/realtime row without localUrl supersedes the temp). Simplest: after successful insert, once the row is persisted, call URL.revokeObjectURL(att.previewUrl) and drop localUrl so ImageBubble/AudioBubble fetch the signed URL.
- [ ] **LOW** `drizzle/0026_messages.sql:25` — messages_client_insert does not verify coach_id belongs to the client's actual coach
  - when: policy messages_client_insert is `FOR INSERT WITH CHECK (client_id=auth.uid() AND sender_role='client')` — coach_id is unconstrained (unlike appt_client_insert which verifies cp.coach_id). A client can insert message rows addressed to an arbitrary coach_id they are not assigned to. The victim coach cannot read them (messages_coach_all is gated on private.is_coach_of(client_id)), so impact is limited to DB pollution and inflated super-admin messages_7d counts, but combined with the coach_id self-assignment hole above the rows become visible as forged history in the victim coach's inbox.
  - fix: Add `AND EXISTS (SELECT 1 FROM client_profiles cp WHERE cp.user_id = auth.uid() AND cp.coach_id = messages.coach_id)` to the WITH CHECK, mirroring appt_client_insert.
- [ ] **LOW** `agents/food-parse/index.v4.ts:1069` — Step 2 recipe-cache check runs serially per item, each doing up to 3 DB round-trips
  - when: A 5-item meal performs up to 15 serial recipe-cache queries on the hot parse path, adding hundreds of ms of avoidable sequential latency on every parse (not only failure cases), and stacking on top of findings #1/#2 toward the timeout.
  - fix: Run the per-item recipe-cache checks with Promise.all (index order preserved), mirroring the composite-decompose Promise.all at line 1093.
- [ ] **LOW** `agents/food-parse/index.v4.ts:1360` — estimateMacrosViaLLM is invoked one item at a time instead of batching, producing N separate LLM calls
  - when: A meal with 4 total DB+decompose misses fires 4 concurrent LLM generations instead of 1, quadrupling token/cost for that parse and increasing the chance of provider 429 rate-limiting under load (which then surfaces as an 'ai_busy'/'rate_limited' failure for the whole request).
  - fix: Collect all dbMissFallbacks into a single estimateMacrosViaLLM(items[]) call and map results back by index, instead of Promise.all over per-item singletons.
- [ ] **LOW** `app/coach/page.tsx:529` — Coach dashboard re-fetches this-week check-ins already contained in the last-7-days result
  - when: checkinsRes (line 469-474) already fetches every habit_checkins row for all clients over the trailing 7 days with `select('*')` (which includes the `completed` and `checked_date` columns). The separate weekCheckins query (line 529-534) then fetches habit_checkins again from this week's Monday with `completed = true` — a strict subset of the rows already in memory (Monday is always within the last 7 days). This is a redundant extra Supabase round-trip and duplicate row transfer on every coach dashboard load; the Mon–Sun dayCounts at lines 537-543 could be computed from the checkinsRes rows already fetched.
  - fix: Delete the weekCheckins query and derive weeklyActivity by filtering the already-fetched checkinsRes.data for completed rows with checked_date >= mondayStr, bucketing by (getDay()+6)%7.
- [ ] **LOW** `app/coach/client/[id]/page.tsx:1387` — Coach client-detail renders raw habit emoji (and hardcoded '📌' fallback) instead of icon mapping
  - when: The coach client-detail page renders habit emoji directly — <span className="text-2xl">{activeHabit.habit.emoji}</span> and similar at lines 1331, 1387, 1772, 1884 — and seeds a hardcoded '📌' default at line 773. Unlike the client dashboard, which converts habit emoji to themed icons via habitEmojiToIcon (app/dashboard/page.tsx:109-114), this screen shows the raw DB emoji, so any habit carrying an emoji renders an emoji in the deployed coach UI, contradicting the de-emoji house rule and the dashboard's own pattern.
  - fix: Reuse the habitEmojiToIcon mapping (or an <Icon>) here as the dashboard does, and drop the hardcoded '📌' literal in favor of a neutral default icon.
- [ ] **LOW** `lib/i18n.tsx:545` — t() replaces only the first placeholder occurrence; adherence.avg_target repeats {unit} and would misrender
  - when: t()'s interpolation uses text.replace(`{${k}}`, ...) (line 1188), and String.prototype.replace with a string pattern replaces only the FIRST occurrence. adherence.avg_target = 'avg {avg}{unit} · target {target}{unit}' (and its es/el/overlay equivalents) contains {unit} twice, so calling t('adherence.avg_target', {avg, unit, target}) would output e.g. 'avg 180g · target 200{unit}' — a literal '{unit}' shown to the user in all 8 languages. The key is currently unused (no code references adherence.*), so this is latent, but it will surface the moment the MacroAdherence copy is wired up, and the same engine limitation breaks any future string that reuses a placeholder.
  - fix: Make interpolation global: text = text.replaceAll(`{${k}}`, String(v)) (or replace(new RegExp('\\{'+k+'\\}','g'), ...)). Note String(v) as a replacement can also misbehave if a value contains '$' — pass a function replacer to be safe.
- [ ] **LOW** `app/api/ai/coach-insight/route.ts:51` — Coach-insight route lacks try/catch — provider/budget/memory failures surface as raw 500s
  - when: A coach requests an insight while the AI provider is down or the org budget is exhausted: executeAiTask() throws and the coach receives an unstyled Next.js 500 with no graceful message, instead of the classified 502 the peer AI routes return.
  - fix: Add a try/catch around the memory-fetch + executeAiTask block returning a 502 with a stable error message; also guard request.json() (e.g. .catch(() => ({})) before safeParse) so a bad body yields 400 not 500.
- [ ] **LOW** `app/api/food/local-search/route.ts:13` — Non-numeric ?limit= yields NaN that defeats the clamp and forces a 500
  - when: A request to /api/food/local-search?q=chicken&limit=abc (or limit=all) produces safeLim=NaN; supabase .limit(NaN) sends an invalid limit to PostgREST, which returns an error, so the route hits the error branch and responds 500 with the raw PostgREST message instead of falling back to the intended default of 15.
  - fix: const parsed = parseInt(...); const limit = Number.isFinite(parsed) ? parsed : 15; then clamp — or const safeLim = Math.min(Math.max(1, Number.isFinite(parsed) ? parsed : 15), 50).
- [ ] **LOW** `app/api/client/message/route.ts:34` — Unguarded request.json() returns a 500 on a malformed body instead of a 400
  - when: An authenticated client POSTs with an empty or truncated body (e.g. a dropped mobile connection mid-request): req.json() throws, and the client sees a 500 rather than the intended 'Invalid message (1-2000 characters)' 400.
  - fix: Parse defensively: const raw = await req.json().catch(() => null); if (!raw) return 400; then safeParse(raw).