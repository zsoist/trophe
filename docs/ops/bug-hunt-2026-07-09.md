# Adversarial Bug Hunt — 2026-07-09

Trigger: operator "full bug hunt ultra code on all trophe … make sure parsing food
is 10/10 ready for b2b". Method: 4 parallel dimension finders (Fable 5) × 2-lens
adversarial verification (default-refute). 25 raw findings → **19 confirmed**, 6
refuted by the verifiers (e.g. a groupBySlot claim killed because its trigger
`setShowSlotConfig(true)` doesn't exist anywhere in the repo).

## Shipped fixes (18/19)

| # | Sev | Bug | Fix | PR |
|---|-----|-----|-----|----|
| 1 | CRIT | messages RLS let a client UPDATE any column (forge coach msg / hijack coach_id) | BEFORE UPDATE trigger freezes all but read_at (mig 0053); **proven live: attack BLOCKED, read_at OK, body intact** | #56 |
| 2-4 | HIGH | 3× cross-tenant IDOR: any org admin read another org's client PHI/meal plans (+LLM cost) via admin bypass | org-aware `canAccessClient()` on all three routes | #56 |
| 5 | HIGH | multi-item parse all-or-nothing (one bad item 422'd the whole meal) | per-item drop, keep the rest, fail only if all bad | #57 |
| 6 | HIGH | qty_g never persisted → grams editor + deterministic recompute dead | persist qty_g on the main insert | #57 |
| 7 | HIGH | erasure left client_invites email/name (no FK, unclassified) | explicit delete by accepted_user_id/email | #59 |
| 8 | HIGH | erasure chat cleanup truncated at 1000 rows | paginate messages query + storage.list | #59 |
| 9 | HIGH | food_log delete never fired (lazy thenable) | void…then() | #58 |
| 10 | HIGH | appearance save clobbered whole display_prefs jsonb | atomic jsonb_set RPC (mig 0054) | #58 |
| 11 | MED | valid clarification (items=[]+question) → opaque failure | return the question | #57 |
| 12 | MED | metabolic scaler ran after barrier + unbounded scale-up | barrier moved after post-processing; scale-up clamped ≤4× | #57 |
| 14 | MED | chat-attachments INSERT policy accepted any uid-prefixed path | require real coach↔client relationship (mig 0053) | #56 |
| 15 | MED | appearance never synced after client-side login | onAuthStateChange | #58 |
| 16 | LOW | photo-analyze leaked ANTHROPIC_API_KEY error verbatim | generic 503 + server log | #56 |
| 17 | LOW | workout week window UTC vs local (off-by-one near midnight) | localDateStr | #58 |
| 18 | LOW | CostsPanel out-of-order fetch overwrote newer data | sequence guard | #58 |
| 19 | LOW | appearance mount-fetch reverted a mid-fetch edit | dirty flag | #58 |

3 migrations applied to prod + journaled (0053, 0054). Landing premium pass = #55.

## Deferred (1/19), with reason

| 13 | MED | no overall deadline on parse `run()` — a 3-item all-DB-miss meal can exceed
Vercel's 60s and 504 | **NOT shipped.** The safe fix (parallelize the serial Step-3
`decomposeAndLookup` loop + deadline budget) reorders the finalItems assembly hot
path and CANNOT be verified without the local canonical-foods DB (:5433, whose
accuracy tests currently red on a stale password). Shipping a blind hot-path
refactor violates the zero-risk B2B posture. Partially self-mitigated: the client
aborts at 45s (< Vercel 60s) so the user sees the handled timeout, not a raw 504,
in the common case; Step 3b (LLM macro estimation) is already parallel. Follow-up:
do it alongside a local-DB refresh, verified by the accuracy suite. |

## Pitfalls captured
- Migrations touching storage.*/auth.* must be schema-guarded (CI = plain Postgres).
- CI-wait bash loops must gate the merge on a captured CI_OK var — a loop `break`
  doesn't set `$?`, so `&&`-chaining a merge after the loop can merge on RED.
- React Compiler: removing an impure call (Date.now()) from a manual useCallback can
  flip a silent compiler bailout into a hard "manual memoization could not be
  preserved" error — drop the manual memo (plain fn) and let the compiler own it.

---

# PART 2 (2026-07-10) — dimensions 5-7 (client-pages, coach-pages, i18n-ux)

The first run covered only 4/7 dimensions (session-limit crash). Ran the
remaining 3 after the limit reset. 18 raw → **14 confirmed** (4 refuted). ALL 14
shipped across PRs #62/#63/#64.

## Logic (PR #62)
| Sev | Bug | Fix |
|-----|-----|-----|
| HIGH | sendCoachMessage reported success on a failed POST (401/500) → silent message loss to coach | check res.ok, throw + show error state |
| HIGH | graduated clients permanently reappeared in Needs-Attention + contact-due (defeats graduation) | exclude graduated_at from both lists |
| MED | batch-assign habit didn't pause existing active → two active client_habits, inconsistent streaks | pause existing active first (in-list) |
| MED | assignHabit paused old habit in DB but never updated pastHabits (vanished till reload) + dup sequence_number | push to pastHabits + fix seq |
| MED | booking time-off matched UTC date vs local weekday → off-by-one west of UTC | localDateStr |
| MED | doCancel optimistic-removed the appt + ignored the update error → client thinks a booked session is cancelled | restore + error on failure |
| LOW | showCalories never threaded MealSlotCard → QuickFoodInput | thread the prop |

## i18n + emoji (PR #63)
5 client-facing components were raw English (non-English B2B clients) and 2 broke
the no-emoji rule: WeeklyCheckin (emoji rating scale → numeric 1-5 + i18n),
BarcodeLookupModal, PainFlagModal, ProgressPhotos, ExerciseComparison all
translated; CoachFoodRecs stopped rendering food-emoji glyph avatars. 59 keys × 8
languages, parity-verified.

## Inbox (PR #64)
| LOW | unread badges computed from the 500-newest slice → quiet clients' unread threads dropped for high-volume coaches | dedicated unread-only query |

Post-deploy: 7 client/coach routes CLEAN. Combined hunt total (parts 1+2):
**33 confirmed bugs, 32 fixed** across PRs #55-64; 1 deferred (parse run() deadline,
needs local accuracy DB).
