> ⚠️ SUPERSEDED by 2026-06-14: deterministic MAPE reduction shipped — v3 700-set now 76.6% pass / 16.0% pooled MAPE (was 76.7%/22.4%); v2 94.3%. See CHANGELOG.md + docs/superpowers/specs/2026-06-14-mape-reduction-deterministic-design.md.

# Trophē — Status Snapshot (2026-06-13)

Single source of truth for "where are we right now." Production: trophe.app ·
Supabase ref iwbpzwmidzvpiofnqexd · `main` @ `898116d` · CI green.

## UPDATE 4 (2026-06-13) — Michael-call waves (built, verified tsc+lint+build)
Decisions: prod 100% DeepSeek (Fable = prior offline-assessment only, disabled by Anthropic ToS);
benchmark official = v2 (210 cases, **94.8%**, cal-MAPE 13.2%, fat 21.3%), backup = v3 (700, 75.6%).
Ledger: docs/coach/michael-call-2026-06-12-requirements.md · research: docs/benchmark/frontier-research-2026-06-13.md.
- **Shipped (non-payment Michael requests):**
  - Per-day **meal-plan macro rollup** (`/api/coach/meal-plan-macros` reuses food-parse `run()`; `MacroRollupModal` + "Macros" button in plan editor) — "the app counts for me".
  - **Clickable meal → recipe breakdown** (✨ on a filled cell opens MealSuggestPicker pre-seeded in Analyze-recipe mode).
  - **Hide calories / weekly-cal-adherence / generic food-ideas from CLIENT view** (`lib/client-view.ts` flags; dashboard calorie hero + progress weekly chart + kcal stats gated). Coach-prescribed recs (CoachFoodRecs) kept.
  - **Custom questionnaire builder** (`/coach/questionnaires` + nav "Intake"; direct writes via 0027 RLS).
  - **Stabilization reframe** of PlateauDetector (neutral framing; goals aren't only weight).
  - **Deterministic calorie equation** (`lib/calorie-equations.ts` Mifflin/Katch → TDEE → macro split; `/api/coach/client-tdee`; "Suggest from body comp" button in plan editor).
  - **Yield/portion library** (`lib/food-yields.ts`: USDA/FAO raw→cooked factors + PN hand-portions + Michael's heuristics) — additive; live-wiring is A/B-gated (prior oil-prompt regressed → reverted; Atwater already in pipeline).
  - **Shopping-list aggregation** finished (`aggregateIngredients` real merge).
- **Already shipped (verified, not rebuilt):** pinned color-coded coach messages top of client dashboard (Phase 0); Atwater/metabolic-consistency reconciliation (v6 in food-parse).
- **Deferred (rationale):** configurable comparison windows (needs 1,800-line client-detail aggregation refactor); graduated/churn surfacing (needs migration + big coach-dashboard UI); pre-appointment message (needs Michael's instruction text + booking UI); live-wiring the yield library (needs benchmark A/B gate); blood-doc upload (Art.9 design). Payment items (Stripe commission, WhatsApp bridge) deferred per instruction.
- **Migration to apply before prod:** `0039_feedback.sql` (feedback widget). All other new routes read existing tables.

## UPDATE 3 (2026-06-13 night) — PWA shipped + beta/pre-revenue fixes
- **PWA LIVE** (Trophē is now installable as a real app, iOS + Android): Serwist service worker
  (offline + NetworkOnly for authed/AI routes), manifest (icons 192/512/maskable/mono + Log-Food/Today
  shortcuts), iOS meta + apple-touch-icon, custom premium install card (Android prompt + iOS Share-sheet
  instructions), branded /offline page, app-polish CSS (safe-area, no-zoom, no-bounce). Prod verified:
  /sw.js (no-cache), /manifest.webmanifest, /icons all 200; CSP preserved. Build switched to `next build
  --webpack` (Serwist requirement) + `turbopack:{}` so dev still runs. App-store note: PWA now; Capacitor wrap later.
- **Beta/pre-revenue fixes shipped**: public /pricing page; ClientComparison real targets (was hardcoded);
  46 TS-schema policies public→authenticated (matches 0008); GDPR consent-withdrawal endpoint (Art.7(3)).
- Deferred (need you/counsel): Supabase Pro backups, DeepSeek Art.46 transfer basis, DPA/DPIA, Stripe billing, EU DB migration.

## UPDATE (later 2026-06-13) — beta-onboarding loop now functional
- **Coach self-onboarding**: invite-code signup `/login?code=` → role from validated DB code (never client input). Michael's code `TROPHE-COACH-MK` live.
- **Coach→client invites**: `/coach/invite` generates a shareable link → `/activate?token=` client signup linked to the coach + mandatory Art.9 consent. Verified (invalid+valid token states), CI green, prod smoke clean.
- **Compliance honesty fix**: Trust page + DPA now state actual hosting = **AWS us-east-2 (US)** + SCCs + planned EU migration (was overclaiming EU residency). EU DB migration recommended for the Greek launch.
- **Latency A1/A2 (partial)**: functions pinned to `cle1` (colocated w/ DB), food_parse max_tokens 2048→1024. Streaming + semantic cache still pending.
- **Correction-capture flywheel** shipped (food_parse_corrections) — labeled-data engine for fine-tuning.

## TL;DR
Production-deployed B2B nutrition-coaching SaaS. Coach module + enterprise/GDPR
layer + 8-language i18n are live. Food-AI is **competitive but not yet "world-class"**:
~90% on the validated 549-set, **76.7% on the harder Greek-weighted 700-set**. The
honest path to the accuracy targets (fine-tuning + Michael validation) is now
*enabled* by the correction-capture flywheel shipped today.

## Benchmark — detailed (v3.9, 700 cases, median-of-3 vs prod, 2026-06-13 08:19)
| Metric | Value |
|---|---|
| **Pass rate** | **537/700 = 76.7%** (validated 549-subset holds ~90%) |
| DB-resolved | 62.3% (629 local_db, 261 llm_cot, 4 hybrid of 896 items) |
| Acc@7.5 (±7.5% all macros — our strict metric) | 25.2% |
| Calorie MAPE | 17.6% (signed −14.2 → slight under) |
| Protein MAPE | 23.6% |
| Carbs MAPE | 21.6% |
| Fat MAPE | 24.8% (the universal hard macro) |
| Latency | p50 4.4s · p95 7.0s (target p95 <3.5s — not met) |

**Per-category pass:** clarification 100% · seafood 95% · base_food 91% · vague_qty 90% ·
adversarial 88% · supplements 83% · bakery 81% · composite 79% · beverages 78% ·
**branded 65% · multi_item 65% · code_switch 61% · regional_cuisine 55%** (weak spots).

**Reading the number honestly:** the 700-set is Greek-weighted (el 195) and deliberately
hard. Much of the regional/code-switch "failure" is (a) genuine Greek-dish difficulty and
(b) the +151 new cases' ranges being research-derived, not yet Michael-validated — taverna
servings have 1.5-2× portion variance. The drift band (DeepSeek server-side, temp=0) is ±1.5-2pt.
Don't quote a single run; quote the band + the metric definition.

## Food database
**42,951 foods**, embedded (Voyage-4, 1024-dim): OFF 21,823 (GR/DE/NL/branded barcodes),
USDA/FNDDS 13,259, CIQUAL 3,323, CoFID 2,744, BEDCA 751, CREA 714, custom 175, HHF 86,
MenuStat 48, chain 28.

## What's live in production
- **Coach module** (Michael call): weekly meal-plan editor, unified messaging (realtime+polling),
  intake questionnaires + daily check-ins, booking, business KPIs + contact-cadence, AI coach
  insight w/ live client snapshot, Super Command Center.
- **Enterprise/GDPR**: public /trust, signable DPA, breach runbook, pricing tiers (draft),
  RLS hardened (migration 0008; TS schema aligned), consent capture, GDPR export endpoint,
  audit-log helper, durable rate limits, API hardening.
- **i18n**: 8 languages (EN/ES/EL inline + FR/DE/IT/PT/NL overlay).
- **Food AI**: v7 DeepSeek-only pipeline (DB-grounded + LLM-CoT hybrid arbitration,
  metabolic-consistency + plausibility caps), 42.9k-food DB.
- **Correction-capture flywheel** (NEW): food_parse_corrections table + food.log.edit capture
  + export script — the labeled-data engine for fine-tuning.

## Scores (full-system audit 2026-06-13, post-hardening)
APIs 9.0 · AI cost 8.7 · AI capabilities 8.5 · Enterprise/security 8.0 · Architecture 7.5 ·
Latency 6.5 · MVP/business 6.0 → **overall ~7.7/10**. (See docs/audits/full-system-audit-2026-06-13.md.)

## Accuracy levers — tried this cycle
- ✅ Greek cooked-vs-raw variant fix (fava 682→218 kcal, gigantes→plaki) + 3 gap seeds → regional 38→55%
- ✅ Benchmark 549→700, Greek-weighted
- ❌ Vector arm (−27pt, reverted) · ❌ aggressive hidden-fat prompt (fat MAPE↑, reverted)
- Pipeline already has the cheap levers (DB-grounding, metabolic-consistency, plausibility)

## The honest path to "world-class" (≥90% on 700 / <10% MAPE)
NOT reachable by prompt/retrieval tweaks (research unanimous; SOTA is 13-25% MAPE). Requires:
1. **Michael validates the Greek case ranges** → makes the 700-set authoritative, likely closes much of 76→90.
2. **Fine-tuning** (FoodyLLM took fat 43→92%) on labeled data → the **correction flywheel** (shipped) generates it; wire the edit UI + reach ≥1,000 corrections → LoRA → A/B.
3. Continued DB coverage (drift-immune) + surgical Greek variant cleanup.

## Known gaps / pending (by owner)
- **You/Michael:** validate Greek benchmark ranges; pricing validation; DPA to EU counsel.
- **You (config):** Supabase Site URL → trophe.app; reconnect Vercel git auto-deploy; Supabase Pro (PITR).
- **Engineering (next):** flywheel edit UI (client + coach), coach-scoped correction path, billing
  enforcement (Stripe), self-serve coach onboarding, coach AI UIs (recipe/meal-plan/shopping),
  god-file splits, nightly-benchmark cron auth fix, p95 latency work.

## CI / deploy
Main CI: **green**. Nightly nutrition-benchmark cron: failing (can't auth to prod in cron — known,
separate from main CI). Vercel git auto-deploy unreliable → manual `vercel --yes --prod`.
