# Local elbow intervention and prepared documented comparator

ELBOW_LOCAL_NATIVE_01 is one intervention from immutable refine-v3-02. The calibrated-reference experiment is closed and not adopted.

Existing final-surface intersection maps identify the local mixed upper-arm/forearm deformation neighborhood. A native Corrective Smooth modifier follows the Armature and precedes masks. It is bound at animated frame 1, with Length Weight smoothing, native factor0.5/iterations5/scale1 defaults, Only Smooth off, Pin Boundaries on. Its group includes intersection-triangle source vertices across the entire base cycle:52 left/51 right. Two adjacency rings feather influence to2/3 and1/3. There is no displacement clamp, weight repaint, authoring change, new solver, or smoothing of the whole arm. The measured maximum displacement is11.288mm; this is an observation, not an acceptance threshold.

The exact original skeleton REST, animated frame1, and evaluated initial surface remain distinct. Initial delta is0.123 micrometers. Across181poses, all18,927vertices outside the231-vertex intervention are unchanged; full authoring/grip/prop matrices are identical. Final evaluated elbow intersections remain: first detected frame38, last125,88frames per side. Wrist regions have zero detected intersections; hand contact retains the prior1mm tolerance. The cycle is6seconds,30fps,180distinct decoded frames at384×480. Position closes exactly; finite-difference endpoint velocities are reported without claiming exact analytic equality.

Decision: limit unresolved. Some smoothing of the fold is visible, but the concentrated crease, angular contour, and surface crossing persist. Do not tune another parameter variation. No consumer candidate or ASSET_READY.

A separate ELBOW_RIGIFY_COMPARATOR_01 is prepared using Blender's bundled Rigify limbs.arm sample/generator with default2limb segments,10B-Bone segments, automatic axis and native automatic weights. The same1999-source-vertex arm surface at baseframe1 is isolated; FK targets replay the original recorded joint trajectory with constant reference alignment. Initial surface delta0.490micrometers, critical joint endpoint delta<0.284micrometers. Three critical before/after pairs have the same isolation boundary, camera and light. The open proximal boundary is an isolated-fixture boundary, not a proposed athlete garment/body change.

This comparator is not a replacement rig for the athlete. Automatic weights and B-Bone distribution change together, so it is not unique causal isolation. It still shows deformation problems; mature-configuration full-cycle surface/grip QA remains pending. No further customized corrective is introduced. Two initial setup preflights failed before generation (addon preference registration, then native bone-collection UI configuration); final setup succeeds without editing Rigify's implementation or installing software. Logs and exact preflight sources are private.

Run the build, temporal, surface, contact, still, clip recipes in that order; the prepared Rigify comparator and its matched baseline render follow independently. Set TROPHE_PROGRAM_ROOT to the private root. Exact executed/private and parameterized/versioned recipes are linked by a hash map. Stable-index analysis keeps Corrective Smooth enabled; final intersection analysis retains all masks and evaluates per-frame triangulation. Noncoplanar/nonadjacent local triangle tests do not certify global collision absence or exercise technique.

Native documentation: https://docs.blender.org/manual/en/latest/modeling/modifiers/deform/corrective_smooth.html and https://docs.blender.org/manual/en/latest/addons/rigify/rig_types/limbs.html . Source versions were read from the actual bundled Blender5.2.1 installation. Human reviews remain pending. Prior sources, calibrated-reference evidence, V2 and consumer schema remain unchanged.
