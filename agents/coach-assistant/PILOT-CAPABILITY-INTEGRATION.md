# Governed capability pilot composition

No live API calls, credential access, cap change or new tables. Existing runner/store/structured transport remain authoritative. Live cap zero still exits before store/transport. Injected usage is simulated diagnostics/cost, never measured spend.

createCapabilityPilotCandidate binds the existing runConversation engine+registry to explicitly supplied synthetic repository/services. It rejects a non-synthetic repository rather than allowing real records through a fixture provider. The test services are the concrete dietary profile transaction service over injected SQL fixtures, not a replacement proposal generator. New explicit development case food_preference_proposal asks naturally for vegetarian preference; it is opt-in and not added to the default four-case smoke. Dataset version coach-pilot-development.v2.

Pilot policy coach-capability-pilot.v1 pins ordered prompts [coach-assistant.capability.v2, coach-assistant.conversation.v5-candidate.1]. Runner verifies each exact invocation prompt and existing Luna/OpenAI/low/2000/maxAttempts1 policy. Other candidates retain their exact single prompt identity for up to two calls. No wildcard/prefix allowlist. Report records allowed sequence and each attempt's actual prompt version.

Each invocation has its own stable attemptId and agentRunId, requestHash, reservation, dispatch claim and settlement. First attempt preserves the old stable ID; second uses an ordinal suffix. Same pilot/evaluation/case replay cannot re-dispatch: the durable claim blocks at invocation one, before service proposal generation. Second invocation cannot occur after unknown model/usage/accounting/dispatch; a candidate catching failure and trying again is explicitly blocked. Maximum two attempted invocations includes repairs; no third dispatch. No automatic resume/replay of model output or proposal creation is introduced.

Case invocations[] contains individual accounting/usage/cost and prompt/ordinal/IDs. Case totals sum completed usage and known settled costs; unknown holds make cost totals null, never zero. Partial known first-call settlement remains visible when the second call is uncertain. Reservations precede EACH provider call. The second prompt includes the canonical service proposal; its request hash binds that content independently.

27 tests pass across runner, existing engine composition and concrete profile transaction tests. Actual engine+registry tests cover two settled calls, first response with unverified model/usage identity (hold/no second call), second transport failure after proposal, replay without another attempt or proposal, and live cap zero before all store/transport. Additional runner test deliberately retries after unknown and attempts a third call; both blocked. Exact prompt mismatch rejected before reservation. Scoped TypeScript/lint pass. All stores/transports and SQL data are injected; durable PostgreSQL/restart/process-race validation, real API quality and human budget approval remain pending. No claims of live measured cost.

Overlap: pilot-runner.ts plus two existing tests; new pilot-capability-candidate.ts. AG1 should apply after earlier runner and engine/registry/profile prerequisites, not replace its whole module or existing accepted bundles. No routes/UI/root/schema altered.
