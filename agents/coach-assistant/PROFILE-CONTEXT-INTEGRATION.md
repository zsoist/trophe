# Relevant stored profile context

New additive response.profileContext contract coach-assistant.profile-context.v1. Existing response.profile duration shape remains compatible. Source is existing client_profiles.workout_preferences via personalContext or durable preference service. Reuses workoutPreferencesSchema.safeParse; never parseWorkoutPreferences defaults. Empty/invalid document returns unknown/null, not confirmed beginner/bodyweight/etc.

Training/profile intent projects only validated experience, equipment, durationMinutes, daysPerWeek, location and document version. Food-only context retains the small prior duration projection. No body measurements, notes, diagnoses, targets, contact information or whole profile is copied. Stored preferences describe declared setup, not proof of today's equipment; an explicit contradictory current user statement should lead to clarification, not overwriting the profile or silently treating history as current authority.

Units source inspection: lib/workout/units.ts persists display preference only in browser localStorage trophe_weight_unit. No authorized server/cross-device unit preference exists. profileContext.units separates storedWeight kg/energy kcal/protein g from preferredWeight:null + not_connected. Optional context.displayWeightUnit kg|lb is strictly validated presentation request only (request_hint), not an authenticated profile fact or authorization. Existing canonical workout weight conversions already calculate kg→lb in tools.ts (e.g.40kg→88.18lb); those typed evidence facts remain the source for explicit conversion requests, not model arithmetic. No new unit persistence or shared lib edit.

Read allocation: existing scope selection determines nutrition relevance. Workout-only exercise turns read plan/workout/exercise plus personalContext (four), instead of reading irrelevant nutrition and dropping profile. Mixed Food+exercise retains all four existing record reads and marks profile capability unknown/profile_omitted_read_budget; it does not silently drop requested nutrition or exceed the cap. Registry none shares the same behavior. Optional pre-selector profile is skipped for mixed exercise turns; otherwise reused once.

Shared memory and outer dietary history bindings now include workoutProfileVersion (canonical validated document plus available server revision), so changing stored preferences invalidates derived history without discarding unchanged continuity. Durable preference source supplies the existing monotonic revision, preserving its ABA protection. Raw repository with no revision can only establish content-change invalidation; no new ABA/durability claim is made for an unavailable revision. Existing dietary/memory versions and current-scope authorization remain included. Failed/foreign profile responses clear profileContext.

44 tests pass across profile, memory/diet brokers, registry none and existing conversations/candidate. New checks cover full declared setup/no-defaults, exercise+profile four-read request with injected single clarification, mixed Food/exercise nutrition preserved and profile omission labeled, existing kg/lb canonical conversion, foreign profile rejection and signed history invalidation after profile change. Clarification prose is explicitly injected; no live model semantic quality claim. Scoped TypeScript/lint pass. No SQL/HTTP/UI/DDL/root/shared lib changes or API spend. AG1 integration and AG4 review pending; units cross-device persistence remains unconnected.
