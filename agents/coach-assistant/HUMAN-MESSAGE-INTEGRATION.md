# Reviewed human-coach message

Prepared product service, no actual message sent. New shared writer and validator under AG1 lease; current /api/client/message and ChatThread are not modified or claimed to use it. Coach AI conversation storage remains separate from public.messages. No SMTP/email/provider/new route.

createCoachMessageService(database, consumeRateLimit) requires the existing real limiter from lib/security/durable-rate-limit. executeCoachMessageAction supplies authenticated current self/org; route binding remains off pending AG1 integration. Operations: message.recipient returns assigned coach UUID, current name (nullable) and scope-bound version; message.propose requires that coach/version and after:{message}; message.apply requires exact proposalId/hash/coachId/resourceVersion/actionId/reviewed:true; message.receipt queries the same action ID. All carry version coach-assistant.v2, conversationId and turnId. No client-supplied sender/organization or AI history is appended.

Validation exactly matches existing quick-message route: trim before min1/max2000, strict object, no added control-character stripping. Proposal presents the normalized exact text and recipient; apply accepts no replacement content. Recipient snapshot/hash binds org/self, assigned coach and full_name plus monotonic relationship revision. Missing name remains null; do not invent a name. Relationship and current org membership/roles are locked and rechecked at apply. Expiry is checked again after rate consumption.

Dedicated private.coach_chat_recipient_versions(subject_id=user_id PK without cascading FK, revision bigint>=0), agreed AG1. Backfill, retain delete/recreate tombstones and increment for assignment/profile changes including old/new owner. Current name changes invalidate the snapshot hash; for name A→B→A invalidation AG1 should increment recipient revisions for clients of the renamed coach. Organization/role membership removals fail current auth; membership ABA invalidation also requires this counter to change for affected subjects. No DDL supplied/executed by AG3; AG1 owns migration/adoption and isolated SQL acceptance. Missing table/column returns not_connected.

Apply locks action ID and proposal. Under the proposal row lock, the service checks durable receipts by actor+proposal ID before consulting the canonical message. A coach deleting the canonical message never resets proposal consumption. Receipts must remain immutable and retained for the proposal lifetime; they must not cascade from messages. Message PK equals proposal ID; a new action ID targeting an already stored proposal returns idempotency_conflict before another rate charge or INSERT. Existing receipt is returned before rate consumption. The same client-message:user bucket enforces 30/900s. Consumed rate tokens may remain consumed after a transaction rollback; this is conservative, not a reset/bypass. The limiter uses its existing separate DB connection and must not be replaced with a permissive production stub.

Message INSERT and durable action receipt commit in ONE caller transaction. Status stored is a historical receipt of persistence at recordedAt, not a claim that the message is still available after deletion, delivered or read. Refresh the current thread to determine present availability. If the commit response is lost, return uncertain and query the existing action receipt; never auto-resend or claim rollback of a committed message. A relationship revoked afterward can deny receipt access; preserve uncertain and do not resend. Same-action receipts remain the source of truth. No attachment upload or push-notification dispatch is included. Refresh the human (coachId, clientId) thread from canonical storage.

Six tests pass: shared route-validator parity/writer scope; concrete SQL double exact content/single-write/same-action recovery after simulated lost commit response/new-action conflict; transaction rollback; assignment ABA/revocation/review/rate rejection. Scoped TypeScript and lint pass. No real PostgreSQL/RLS/HTTP/Realtime or delivery test. AG1 must verify concurrent action/proposal replay, current relationship/org/identity revision triggers, rate bucket continuity, real insert+receipt rollback/recovery and user review flow before enabling. US$0; no real recipients contacted.
